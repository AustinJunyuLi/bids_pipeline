---
phase: quick
plan: 260330-o5x
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/models.py
  - skill_pipeline/deal_agent.py
  - skill_pipeline/db_load.py
  - tests/test_skill_pipeline.py
  - tests/test_skill_db_load.py
  - tests/test_skill_db_export.py
  - tests/test_skill_mirror_sync.py
  - CLAUDE.md
  - .claude/skills/reconcile-alex/SKILL.md
  - .codex/skills/reconcile-alex/SKILL.md
  - .cursor/skills/reconcile-alex/SKILL.md
autonomous: true
requirements: []
---

<objective>
Repair the mandatory interpretive enrich-deal contract so the live pipeline
matches the intended philosophy:

- `enrich-core` remains authoritative for deterministic enrichment
- `/enrich-deal` is a mandatory interpretive layer after deterministic enrichment
- `db-load` and `deal-agent` enforce that split consistently
- canonical and mirrored skill docs describe the same runtime contract
</objective>

<context>
The repo had drifted into a confused state:

- `enrichment.json` was documented as interpretive-only in the live skill docs
  but still validated as if it owned deterministic fields
- `deal-agent` crashed on valid 5-key interpretive artifacts
- `db-load` required `enrichment.json` to exist but did not fail closed on a
  malformed interpretive payload
- reconcile docs still described `enrichment.json` as full enrichment
</context>

<tasks>

<task>
  <name>Split runtime enrichment models by responsibility</name>
  <files>skill_pipeline/models.py</files>
  <action>
    Introduce a deterministic enrichment model for rounds, bid classifications,
    cycles, formal boundary, sparse deterministic dropout labels, and
    all-cash overrides. Keep the public `SkillEnrichmentArtifact` name aligned
    with the interpretive-only 5-key `enrichment.json` contract.
  </action>
</task>

<task>
  <name>Enforce the split in deal-agent and db-load</name>
  <files>skill_pipeline/deal_agent.py, skill_pipeline/db_load.py</files>
  <action>
    Make `deal-agent` summarize deterministic counts from
    `deterministic_enrichment.json` and interpretive judgments from
    `enrichment.json`, returning FAIL rather than crashing on malformed
    interpretive data. Make `db-load` validate both artifacts with the correct
    models and fail closed on incomplete interpretive payloads.
  </action>
</task>

<task>
  <name>Refresh tests and official docs</name>
  <files>
    tests/test_skill_pipeline.py,
    tests/test_skill_db_load.py,
    tests/test_skill_db_export.py,
    tests/test_skill_mirror_sync.py,
    CLAUDE.md,
    .claude/skills/reconcile-alex/SKILL.md
  </files>
  <action>
    Update fixtures to reflect the split artifacts, add regressions for malformed
    interpretive enrichment, sync skill mirrors, and record the contract in the
    authoritative repo docs.
  </action>
</task>

</tasks>

<verification>
python3 -m pytest -q tests/test_skill_pipeline.py tests/test_skill_db_load.py tests/test_skill_db_export.py tests/test_skill_enrich_core.py tests/test_runtime_contract_docs.py tests/test_skill_mirror_sync.py tests/test_benchmark_separation_policy.py
</verification>

<success_criteria>
- Deterministic enrichment ownership remains in `deterministic_enrichment.json`
- `enrichment.json` is enforced as the 5-key interpretive artifact
- `deal-agent` no longer crashes on a valid interpretive-only artifact
- `db-load` rejects malformed interpretive enrichment instead of silently
  accepting it
- canonical and mirrored reconcile docs reflect the split contract
</success_criteria>

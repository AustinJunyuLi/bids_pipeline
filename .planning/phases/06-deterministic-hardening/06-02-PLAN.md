---
phase: 06-deterministic-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - skill_pipeline/coverage.py
  - skill_pipeline/gates.py
  - tests/test_skill_coverage.py
  - tests/test_skill_gates.py
autonomous: true
requirements:
  - HARD-04

must_haves:
  truths:
    - "coverage no longer classifies rollover-side, teaming, or diligence confidentiality references as sale-process NDA cues"
    - "gates ignore non-sale-process NDA events when evaluating nda_after_drop and nda_signer_no_downstream"
    - "Legitimate bidder-to-target sale-process NDAs still behave as NDAs in both coverage and gates"
    - "The interim Phase 6 exclusion remains text-pattern based and does not change the artifact schema"
  artifacts:
    - path: "skill_pipeline/coverage.py"
      provides: "Evidence-item cue classification for NDA coverage findings"
      contains: "def _classify_cue_family"
    - path: "skill_pipeline/gates.py"
      provides: "Cross-event and actor-lifecycle gate rules for NDA behavior"
      contains: "def _gate_cross_event_logic"
    - path: "tests/test_skill_coverage.py"
      provides: "Coverage regressions for excluded vs genuine confidentiality-agreement cues"
      min_lines: 200
    - path: "tests/test_skill_gates.py"
      provides: "Gate regressions for excluded vs genuine NDA events"
      min_lines: 250
  key_links:
    - from: "skill_pipeline/gates.py"
      to: "skill_pipeline/coverage.py"
      via: "shared Phase 6 NDA qualification phrases"
      pattern: "confidentiality"
    - from: "skill_pipeline/gates.py"
      to: "tests/test_skill_gates.py"
      via: "nda_after_drop and nda_signer_no_downstream regressions"
      pattern: "nda_after_drop"
---

<objective>
Ship the interim Phase 6 confidentiality-agreement tolerance fix across both
coverage and gates so rollover-side, bidder-bidder teaming, and target-on-target
diligence agreements stop producing false NDA findings without changing the
artifact schema.

Purpose: HARD-04 is not a schema redesign. It is a conservative text-pattern
filter that suppresses known false positives while preserving real sale-process
NDA signals until Phase 8 adds `nda_subtype`.

Output: tighter NDA cue classification in `coverage.py`, non-sale NDA exclusion
inside `gates.py`, and positive/negative regression tests in the existing
coverage and gates suites.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deterministic-hardening/06-CONTEXT.md
@.planning/phases/06-deterministic-hardening/06-RESEARCH.md
@.planning/phases/06-deterministic-hardening/06-VALIDATION.md

@skill_pipeline/coverage.py
@skill_pipeline/gates.py
@skill_pipeline/extract_artifacts.py
@skill_pipeline/pipeline_models/source.py
@tests/test_skill_coverage.py
@tests/test_skill_gates.py

<interfaces>
From `skill_pipeline/coverage.py`:
```python
def _classify_cue_family(item: EvidenceItem) -> str | None: ...
```

From `skill_pipeline/gates.py`:
```python
def _event_block_ids(artifacts: LoadedExtractArtifacts, event) -> list[str]: ...
def _gate_cross_event_logic(sorted_events: list, cycle_ranges: list[tuple[int, int]], undated_event_ids: list[str]) -> list[GateFinding]: ...
def _gate_actor_lifecycle(actors: list, events: list) -> list[GateFinding]: ...
```

Existing gate rule IDs that must remain stable:
```text
nda_after_drop
nda_signer_no_downstream
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend coverage cue classification with explicit non-sale NDA exclusions</name>
  <files>skill_pipeline/coverage.py, tests/test_skill_coverage.py</files>
  <read_first>
    - skill_pipeline/coverage.py (current NDA language detection and existing contextual exclusions)
    - tests/test_skill_coverage.py (fixture helper and confidence/cue assertions)
    - .planning/phases/06-deterministic-hardening/06-CONTEXT.md (D-07 through D-09)
  </read_first>
  <acceptance_criteria>
    - `skill_pipeline/coverage.py` contains a dedicated non-sale NDA exclusion helper or equivalent inline predicate
    - the exclusion checks for exact lower-cased substrings covering rollover, teaming, and diligence language
    - `pytest -q tests/test_skill_coverage.py -k "rollover or diligence or nda"` exits `0`
    - a genuine bidder NDA still yields `cue_family == "nda"` in a negative-control test
  </acceptance_criteria>
  <action>
    Update `skill_pipeline/coverage.py` so NDA classification requires both:
    - ordinary NDA/confidentiality language
    - absence of non-sale-process markers

    Implement the Phase 6 exclusion using exact normalized substrings, including:
    - rollover / management equity markers:
      - `management rollover`
      - `rollover equity`
      - `rolled over`
      - `continuing equity`
    - bidder teaming markers:
      - `joint bid`
      - `consortium`
      - `teaming`
      - `partnered with`
    - non-sale diligence markers:
      - `due diligence with respect to`
      - `engage in due diligence with respect to`

    Keep the existing prior-executed NDA exclusions intact. Add tests in
    `tests/test_skill_coverage.py` for:
    - rollover confidentiality language -> not classified as `nda`
    - diligence confidentiality language -> not classified as `nda`
    - standard bidder confidentiality agreement with the target -> still classified as `nda`
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same non-sale NDA filter to gates before lifecycle and cycle checks</name>
  <files>skill_pipeline/gates.py, tests/test_skill_gates.py</files>
  <read_first>
    - skill_pipeline/gates.py (current `nda_after_drop` and `nda_signer_no_downstream` behavior)
    - skill_pipeline/coverage.py (the exact Phase 6 exclusion phrases added in Task 1)
    - tests/test_skill_gates.py (quote-first and canonical fixture patterns)
    - skill_pipeline/pipeline_models/source.py (ChronologyBlock text fields)
  </read_first>
  <acceptance_criteria>
    - `skill_pipeline/gates.py` contains a helper that evaluates NDA qualification from event summary and/or matched block text
    - `skill_pipeline/gates.py` skips non-sale NDA events before updating dropped-actor or downstream-NDA state
    - `pytest -q tests/test_skill_gates.py -k "nda_after_drop or nda_signer_no_downstream"` exits `0`
    - a rollover-style NDA does not produce `nda_after_drop`
    - a standard bidder NDA still can produce `nda_after_drop` and `nda_signer_no_downstream` in negative-control tests
  </acceptance_criteria>
  <action>
    In `skill_pipeline/gates.py`, add a Phase 6 helper that:
    - gathers normalized text from `event.summary`
    - appends normalized `raw_text`/`clean_text` from chronology blocks returned by `_event_block_ids(...)`
    - returns `False` for sale-process qualification when the text contains the
      exact non-sale substrings listed in Task 1

    Then use that helper in both places where NDA events currently drive logic:
    - `_gate_cross_event_logic()` -> do not trigger `nda_after_drop` for excluded NDA events
    - `_gate_actor_lifecycle()` -> do not add excluded NDA actors into the `nda_signer_ids` set

    Add regressions in `tests/test_skill_gates.py` for:
    - quote-first rollover NDA after a drop -> no `nda_after_drop`
    - canonical rollover-only signer -> no `nda_signer_no_downstream`
    - normal bidder NDA after a drop -> existing blocker remains
    - normal bidder NDA signer with no downstream events -> existing warning remains
  </action>
</task>

</tasks>

---
phase: 10-pre-migration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/enrich_core.py
  - tests/test_skill_enrich_core.py
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "An executed event without explicit consideration type no longer suppresses fallback all-cash inference for the same cycle"
    - "The Providence negative guardrail still holds when cash is not unambiguous from the proposal set"
    - "Regression coverage reproduces the Saks-style short-circuit bug and verifies the fixed behavior"
  artifacts:
    - path: "skill_pipeline/enrich_core.py"
      provides: "Fixed all-cash inference logic"
    - path: "tests/test_skill_enrich_core.py"
      provides: "Regression tests covering the executed-without-type short-circuit"
  key_links:
    - from: "skill_pipeline/enrich_core.py"
      to: "tests/test_skill_enrich_core.py"
      via: "_infer_all_cash_overrides() regression coverage"
      pattern: "Code path and tests prove the same control-flow fix"
---

<objective>
Fix the `_infer_all_cash_overrides()` short-circuit bug in isolation and prove
the new behavior with deterministic tests.

Purpose: Phase 10 must establish a clean corrected v1 baseline before any v2
model work begins. This plan changes only the all-cash inference helper and its
tests.

Output: Updated inference logic plus regression coverage for the Saks-style
executed-without-type cycle.
</objective>

<context>
@.planning/phases/10-pre-migration-fixes/10-CONTEXT.md
@.planning/phases/10-pre-migration-fixes/10-RESEARCH.md
@.planning/ROADMAP.md
@skill_pipeline/enrich_core.py
@tests/test_skill_enrich_core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch all-cash fallback control flow</name>
  <files>
    skill_pipeline/enrich_core.py
  </files>
  <action>
Update `_infer_all_cash_overrides()` so an `executed` event with missing
`consideration_type` no longer exits the cycle early before the typed-proposal
fallback can run.

Keep the scope narrow:
- preserve the cash-executed fast path
- preserve explicit mixed/non-cash guardrails
- require enough typed-cash proposal evidence that Providence-style sparse
  evidence does not flip the whole cycle to all-cash
  </action>
  <acceptance_criteria>
    - `_infer_all_cash_overrides()` still returns `{}` when there is no
      unambiguous cycle-local cash evidence
    - cycles with multiple typed cash proposals and an executed event lacking
      type can still infer overrides for untyped proposals
  </acceptance_criteria>
  <done>All-cash inference no longer short-circuits on executed-without-type cycles</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for the fixed path and the preserved guardrail</name>
  <files>
    tests/test_skill_enrich_core.py
  </files>
  <action>
Add or update tests to cover:
- a Saks-like cycle containing at least two typed cash proposals, untyped
  proposals, and an executed event with null consideration type
- the existing Providence guardrail staying negative
  </action>
  <verify>
    <automated>pytest -q tests/test_skill_enrich_core.py -k all_cash</automated>
  </verify>
  <acceptance_criteria>
    - the new regression test fails against the old implementation and passes
      with the fix
    - the Providence guardrail test remains green
  </acceptance_criteria>
  <done>Regression coverage proves the short-circuit fix without widening scope too far</done>
</task>

</tasks>

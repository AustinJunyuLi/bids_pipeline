---
phase: 10-pre-migration-fixes
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - data/skill/saks/enrich/deterministic_enrichment.json
  - data/skill/saks/export/deal_events.csv
  - data/pipeline.duckdb
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Affected deal exports are regenerated after the all-cash fix"
    - "The post-fix baseline is re-measured only after refreshed exports exist"
    - "Phase 10 bookkeeping captures the corrected baseline as the starting point for v2 work"
  artifacts:
    - path: "data/skill/saks/enrich/deterministic_enrichment.json"
      provides: "Concrete post-fix all_cash_overrides output for the known affected deal"
    - path: "data/skill/saks/export/deal_events.csv"
      provides: "Refreshed export carrying the all-cash correction downstream"
    - path: ".planning/phases/10-pre-migration-fixes/10-VERIFICATION.md"
      provides: "Phase-level verification and baseline note"
  key_links:
    - from: "data/skill/saks/enrich/deterministic_enrichment.json"
      to: "data/skill/saks/export/deal_events.csv"
      via: "db-load -> db-export"
      pattern: "all_cash_overrides flow into the benchmark-facing CSV"
---

<objective>
Materialize the Phase 10 fixes in generated artifacts and record the corrected
benchmark/export baseline before Phase 11 begins.

Purpose: The migration roadmap requires a clean attribution boundary. This plan
reruns the affected downstream artifacts, then records the post-fix baseline in
Phase 10 verification/state bookkeeping.

Output: Refreshed deterministic/export artifacts for affected deals plus Phase
10 verification and progress updates.
</objective>

<context>
@.planning/phases/10-pre-migration-fixes/10-CONTEXT.md
@.planning/phases/10-pre-migration-fixes/10-RESEARCH.md
@.claude/skills/reconcile-alex/SKILL.md
@skill_pipeline/db_load.py
@skill_pipeline/db_export.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run affected downstream artifacts after the all-cash fix</name>
  <files>
    data/skill/saks/enrich/deterministic_enrichment.json,
    data/skill/saks/export/deal_events.csv,
    data/pipeline.duckdb
  </files>
  <action>
After the Phase 10 code/test changes land, rerun the minimum downstream stages
needed to materialize the corrected all-cash behavior on the known affected
deal set.

At minimum:
- `skill-pipeline enrich-core --deal saks`
- `skill-pipeline db-load --deal saks`
- `skill-pipeline db-export --deal saks`

If additional deals show the same short-circuit pattern under inspection, rerun
their downstream stages too.
  </action>
  <acceptance_criteria>
    - refreshed deterministic enrichment exists for every rerun deal
    - refreshed exports exist for every rerun deal
    - observed deterministic diffs are confined to the intended all-cash path
  </acceptance_criteria>
  <done>Affected downstream artifacts now reflect the Phase 10 all-cash fix</done>
</task>

<task type="auto">
  <name>Task 2: Record the corrected baseline and phase completion bookkeeping</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/10-pre-migration-fixes/10-VERIFICATION.md
  </files>
  <action>
After refreshed exports exist, re-measure the benchmark-facing baseline using
the existing post-export workflow, then record the outcome in Phase 10
verification/state artifacts.

Bookkeeping requirements:
- update Phase 10 verification with the actual rerun evidence
- update roadmap/state progress to show Phase 10 complete when all plans are
  executed
- note the corrected baseline explicitly so Phase 11 starts from the new value
  </action>
  <acceptance_criteria>
    - Phase 10 verification cites the refreshed downstream artifacts
    - roadmap/state progress reflect executed Phase 10 plans
    - the corrected baseline is documented before any Phase 11 work begins
  </acceptance_criteria>
  <done>Phase 10 closes with a documented corrected v1 baseline</done>
</task>

</tasks>

---
phase: 10-pre-migration-fixes
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - skill_pipeline/models.py
  - skill_pipeline/coverage.py
  - tests/test_skill_coverage.py
autonomous: true
requirements: [FIX-02]

must_haves:
  truths:
    - "The live model surface defines a structured CoverageCheckRecord with explicit status and reason-code fields"
    - "The deterministic coverage stage emits structured detail records with supporting references while preserving summary gate behavior"
    - "Coverage tests verify both uncovered and covered cue outputs under the new contract"
  artifacts:
    - path: "skill_pipeline/models.py"
      provides: "CoverageCheckRecord model and updated coverage artifact type"
    - path: "skill_pipeline/coverage.py"
      provides: "Structured coverage record generation"
    - path: "tests/test_skill_coverage.py"
      provides: "Regression tests for the structured coverage contract"
  key_links:
    - from: "skill_pipeline/coverage.py"
      to: "skill_pipeline/models.py"
      via: "CoverageCheckRecord serialization"
      pattern: "coverage detail artifact matches the typed model"
---

<objective>
Replace the ad hoc coverage finding detail contract with structured
`CoverageCheckRecord` outputs while keeping the current coverage summary gate
stable.

Purpose: Phase 10 should establish the structured coverage contract before v2
reuses the concept. This is a v1 runtime/output upgrade, not a full extraction
schema rewrite.

Output: Typed coverage detail records with `status`, `reason_code`, and
supporting references, plus updated tests.
</objective>

<context>
@.planning/phases/10-pre-migration-fixes/10-CONTEXT.md
@.planning/phases/10-pre-migration-fixes/10-RESEARCH.md
@skill_pipeline/models.py
@skill_pipeline/coverage.py
@tests/test_skill_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CoverageCheckRecord to the live v1 model surface</name>
  <files>
    skill_pipeline/models.py
  </files>
  <action>
Define a structured `CoverageCheckRecord` with explicit coverage status and
supporting-reference fields, then point the coverage detail artifact wrapper at
that model.

Keep the model compatible with current v1 coverage needs:
- status values must include observed/derived/not_found/ambiguous
- records must carry reason codes and supporting references
- summary models remain unchanged
  </action>
  <acceptance_criteria>
    - `skill_pipeline/models.py` exports a typed coverage-check model usable by
      the coverage stage
    - the detail artifact wrapper serializes lists of structured checks
  </acceptance_criteria>
  <done>Coverage detail records now have an explicit typed contract</done>
</task>

<task type="auto">
  <name>Task 2: Emit structured coverage records from the deterministic coverage stage</name>
  <files>
    skill_pipeline/coverage.py
  </files>
  <action>
Refactor coverage detail generation so each emitted record includes structured
status/reason/supporting-reference information rather than only a prose finding.

Preserve existing pass/fail semantics:
- enrich-core still gates on `coverage_summary.json`
- high-confidence missing critical cues still count as errors
- medium-confidence gaps still count as warnings
  </action>
  <acceptance_criteria>
    - covered cues can emit `observed` records with supporting references
    - missing actionable cues emit `not_found` records with reason codes
    - the summary still reports the same pass/fail counts expected by downstream
      code
  </acceptance_criteria>
  <done>coverage.py writes structured checks without breaking summary gating</done>
</task>

<task type="auto">
  <name>Task 3: Update coverage tests to the new structured contract</name>
  <files>
    tests/test_skill_coverage.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - tests assert `status`/`reason_code` for uncovered cues
    - tests assert supporting references for covered cues
    - the full coverage test slice passes
  </acceptance_criteria>
  <done>Coverage tests lock the new structured contract in place</done>
</task>

</tasks>

---
phase: 01-workflow-contract-surface
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/codebase/WORKFLOW.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - RISK-02
must_haves:
  truths:
    - "A future planner can recover the active brownfield workflow and its deterministic vs LLM split from `.planning/` without private notes."
    - "The planning surface treats the current hybrid architecture as the validated baseline for later phases, not as a rewrite placeholder."
  artifacts:
    - path: .planning/codebase/WORKFLOW.md
      provides: "Planner-facing workflow memory with stage ownership, artifact handoffs, and failure boundaries"
    - path: .planning/PROJECT.md
      provides: "Project memory that points future work at the committed workflow baseline"
  key_links:
    - from: .planning/codebase/WORKFLOW.md
      to: docs/design.md
      via: "public-doc to planning-memory cross-reference"
      pattern: "docs/design.md"
    - from: .planning/PROJECT.md
      to: .planning/codebase/WORKFLOW.md
      via: "durable project-memory reference"
      pattern: "WORKFLOW.md"
---

<objective>
Capture the stabilized brownfield workflow inside `.planning/` for future phases.

Purpose: Satisfy the project-memory requirement by making later planning independent of private notes or oral context.
Output: A planner-facing workflow memory document plus updated project memory that references it.
</objective>

<execution_context>
@/home/austinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/austinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
@docs/design.md
@CLAUDE.md
@.claude/skills/README.md
@skill_pipeline/cli.py
@skill_pipeline/paths.py
@skill_pipeline/models.py

<interfaces>
From `skill_pipeline/models.py`:
```python
class SkillPathSet(SkillModel):
    source_dir: Path
    chronology_blocks_path: Path
    evidence_items_path: Path
    document_registry_path: Path
    extract_dir: Path
    check_dir: Path
    verify_dir: Path
    coverage_dir: Path
    enrich_dir: Path
    export_dir: Path
    actors_raw_path: Path
    events_raw_path: Path
    spans_path: Path
    check_report_path: Path
    verification_log_path: Path
    coverage_summary_path: Path
    deterministic_enrichment_path: Path
    enrichment_path: Path
    deal_events_path: Path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create planner-facing workflow memory in `.planning/codebase/WORKFLOW.md`</name>
  <files>.planning/codebase/WORKFLOW.md</files>
  <read_first>
.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
docs/design.md
CLAUDE.md
.claude/skills/README.md
skill_pipeline/cli.py
skill_pipeline/paths.py
skill_pipeline/models.py
  </read_first>
  <action>Create `.planning/codebase/WORKFLOW.md` as the planner-facing brownfield workflow map. Make the deterministic vs LLM/skill-driven mix explicit, list the stage order and owner surface for each stage, capture the exact artifact roots and handoff files from `SkillPathSet`, and record the major fail-fast boundaries that later phases must preserve. Include the fact that there is no single tracked end-to-end runner today and that `deal-agent` is only a preflight/summary surface.</action>
  <acceptance_criteria>
- `.planning/codebase/WORKFLOW.md` exists and names every active stage in order.
- The document distinguishes deterministic CLI stages, LLM skills, hybrid repair, formatting-only export, and post-export diagnostics.
- The doc references the exact artifact roots and handoff files used by `build_skill_paths()` / `SkillPathSet`.
- The absence of a single tracked end-to-end runner is stated explicitly.
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path(".planning/codebase/WORKFLOW.md").read_text(encoding="utf-8")
required = [
    "deterministic",
    "LLM",
    "hybrid",
    "formatting",
    "post-export",
    "skill-pipeline deal-agent",
    "raw/<slug>/",
    "data/deals/<slug>/source/",
    "data/skill/<slug>/extract/",
    "verification_log.json",
    "deterministic_enrichment.json",
    "deal_events.csv",
]
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit(f"Missing workflow memory terms: {missing}")
PY</automated>
  </verify>
  <done>The planning surface has a durable workflow-memory file that future phases can use as the brownfield baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Update `.planning/PROJECT.md` to point future work at the workflow memory</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/codebase/WORKFLOW.md
docs/design.md
CLAUDE.md
  </read_first>
  <action>Update `.planning/PROJECT.md` so the committed project memory explicitly points contributors and future planners at `docs/design.md` for the public workflow contract and `.planning/codebase/WORKFLOW.md` for planner-facing brownfield memory. Record that the deterministic vs LLM/skill-driven split is an intentional current constraint, not a temporary doc gap, and keep the active-context section aligned with the Phase 1 outcome.</action>
  <acceptance_criteria>
- `.planning/PROJECT.md` references `.planning/codebase/WORKFLOW.md`.
- The project memory says future work should treat the hybrid deterministic vs skill-driven split as the current baseline.
- The updated project memory is sufficient to recover the repo's active workflow without private notes.
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
project = Path(".planning/PROJECT.md").read_text(encoding="utf-8")
required = [
    ".planning/codebase/WORKFLOW.md",
    "docs/design.md",
    "deterministic",
    "skill-driven",
    "brownfield",
]
missing = [item for item in required if item not in project]
if missing:
    raise SystemExit(f"Missing project-memory terms: {missing}")
PY</automated>
  </verify>
  <done>The core planning docs now preserve the workflow contract and next-step context without relying on private local notes.</done>
</task>

</tasks>

<verification>
Run the two task verification commands and confirm `.planning/codebase/WORKFLOW.md` is referenced from `.planning/PROJECT.md`.
</verification>

<success_criteria>
- `.planning/` contains a planner-facing workflow memory document.
- `.planning/PROJECT.md` points future work at that committed memory.
- Later phases can recover the hybrid workflow baseline without private notes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-contract-surface/01-03-SUMMARY.md`
</output>

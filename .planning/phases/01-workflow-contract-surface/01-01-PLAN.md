---
phase: 01-workflow-contract-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workflow-contract.md
  - docs/design.md
  - tests/test_workflow_contract_surface.py
autonomous: true
requirements:
  - WFLO-01
user_setup: []
must_haves:
  truths:
    - "Contributors can read one committed contract document and see the full stage order from raw-fetch through /export-csv."
    - "The contract document states which stages are deterministic CLI stages, which are LLM or skill stages, and which stage is hybrid."
    - "The contract surface distinguishes `skill-pipeline deal-agent` from `/deal-agent` without requiring code inspection."
  artifacts:
    - path: docs/workflow-contract.md
      provides: "Single-document workflow inventory with stage classification, artifacts, and gate boundaries"
      min_lines: 80
      contains: "Deterministic vs LLM Mix"
    - path: docs/design.md
      provides: "Current-runtime design index that points to the contract doc instead of acting as the only detailed source"
      min_lines: 20
      contains: "docs/workflow-contract.md"
    - path: tests/test_workflow_contract_surface.py
      provides: "Regression coverage for the committed workflow contract"
      min_lines: 40
      contains: "workflow-contract.md"
  key_links:
    - from: docs/design.md
      to: docs/workflow-contract.md
      via: "explicit reference to the detailed contract doc"
      pattern: "workflow-contract\\.md"
    - from: tests/test_workflow_contract_surface.py
      to: docs/workflow-contract.md
      via: "doc assertions over current stage order and stage type language"
      pattern: "workflow-contract\\.md"
---

<objective>
Create a single committed workflow contract that inventories every live pipeline stage and makes the deterministic versus LLM split explicit.

Purpose: Phase 1 cannot succeed while the active workflow is spread across code, CLAUDE.md, and skill docs with no single contract surface.
Output: A durable workflow-contract doc, a lighter design index that points to it, and regression coverage that fails if the contract surface drifts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
@CLAUDE.md
@docs/design.md
@skill_pipeline/cli.py
@skill_pipeline/deal_agent.py
@.claude/skills/README.md
@.claude/skills/deal-agent/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical workflow contract document</name>
  <files>docs/workflow-contract.md</files>
  <read_first>.planning/phases/01-workflow-contract-surface/01-RESEARCH.md, CLAUDE.md, docs/design.md, skill_pipeline/cli.py, skill_pipeline/deal_agent.py, .claude/skills/deal-agent/SKILL.md, .claude/skills/README.md</read_first>
  <action>Create `docs/workflow-contract.md` as the single detailed inventory for the live pipeline. Include the exact stage order `raw-fetch -> preprocess-source -> /extract-deal -> canonicalize -> check -> verify -> coverage -> /verify-extraction -> enrich-core -> /enrich-deal -> /export-csv`, with `/reconcile-alex` documented as optional and post-export only. Add a stage table with columns for entrypoint, type (`Deterministic`, `LLM skill`, `Hybrid repair`, `Formatting only`), required inputs, produced artifacts, and fail-fast gate behavior. Include a short count summary for deterministic versus LLM stages and explicitly distinguish `skill-pipeline deal-agent` as preflight/summary-only from `/deal-agent` as the orchestrating skill command.</action>
  <verify>rg -n "Deterministic vs LLM Mix|skill-pipeline deal-agent|/deal-agent|/verify-extraction|/export-csv" docs/workflow-contract.md</verify>
  <acceptance_criteria>
    - docs/workflow-contract.md contains the exact heading "Deterministic vs LLM Mix"
    - docs/workflow-contract.md contains the full ordered sequence from "raw-fetch" through "/export-csv"
    - docs/workflow-contract.md contains both "skill-pipeline deal-agent" and "/deal-agent"
    - docs/workflow-contract.md contains "post-export only" for "/reconcile-alex"
  </acceptance_criteria>
  <done>The repo has one committed contract doc that makes the stage mix and handoff sequence explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce docs/design.md to an accurate current-runtime index</name>
  <files>docs/design.md</files>
  <read_first>docs/design.md, docs/workflow-contract.md, CLAUDE.md</read_first>
  <action>Update `docs/design.md` so it stays a concise current-runtime index rather than a second competing detailed contract. Keep the active runtime callout, the historical-doc warning, and a compact sequence summary, but add an explicit pointer to `docs/workflow-contract.md` as the detailed source for stage classification, artifact paths, and gate boundaries. Remove or rewrite any wording that implies `docs/design.md` alone is sufficient to recover the full contract.</action>
  <verify>rg -n "workflow-contract.md|seed-only `skill_pipeline` hybrid workflow|historical background" docs/design.md</verify>
  <acceptance_criteria>
    - docs/design.md contains "docs/workflow-contract.md"
    - docs/design.md still contains "seed-only `skill_pipeline` hybrid workflow"
    - docs/design.md still warns that the rewrite-plan docs are historical background
  </acceptance_criteria>
  <done>The design index is accurate, concise, and points readers to the detailed contract doc.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for the workflow contract surface</name>
  <files>tests/test_workflow_contract_surface.py</files>
  <read_first>tests/test_benchmark_separation_policy.py, docs/workflow-contract.md, docs/design.md, CLAUDE.md</read_first>
  <action>Add `tests/test_workflow_contract_surface.py` with document-level assertions that protect the active contract surface. At minimum, assert that `docs/workflow-contract.md` exists, states the current end-to-end stage order, mentions the deterministic versus LLM split, documents `/verify-extraction` as a repair step after deterministic `verify` and `coverage`, and distinguishes `skill-pipeline deal-agent` from `/deal-agent`. Also assert that `docs/design.md` references `docs/workflow-contract.md` so the detailed contract remains discoverable from the design index.</action>
  <verify>pytest -q tests/test_workflow_contract_surface.py</verify>
  <acceptance_criteria>
    - tests/test_workflow_contract_surface.py exists
    - tests/test_workflow_contract_surface.py contains "skill-pipeline deal-agent"
    - tests/test_workflow_contract_surface.py contains "workflow-contract.md"
    - `pytest -q tests/test_workflow_contract_surface.py` exits 0
  </acceptance_criteria>
  <done>A focused regression test fails if the workflow contract surface drifts or becomes ambiguous again.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_workflow_contract_surface.py` passes
- [ ] `rg -n "Deterministic vs LLM Mix|skill-pipeline deal-agent|/deal-agent" docs/workflow-contract.md` returns matches
- [ ] `rg -n "workflow-contract.md" docs/design.md` returns a match
</verification>

<success_criteria>

- All tasks completed
- The repo has one committed detailed workflow contract doc for the live pipeline
- The contract doc makes the deterministic, LLM, hybrid, and formatting-only stages explicit
- A regression test protects the contract surface from silent drift
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-contract-surface/01-01-SUMMARY.md`
</output>

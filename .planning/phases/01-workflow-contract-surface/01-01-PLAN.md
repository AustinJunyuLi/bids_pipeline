---
phase: 01-workflow-contract-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design.md
  - tests/test_workflow_contract_docs.py
autonomous: true
requirements:
  - WFLO-01
must_haves:
  truths:
    - "A contributor can open the readme-level workflow doc and see the full stage order from raw-fetch through post-export reconciliation."
    - "A contributor can tell which stages are deterministic CLI stages and which are LLM or skill-driven without reading Python code."
  artifacts:
    - path: docs/design.md
      provides: "Contributor-facing workflow contract with stage inventory, artifact roots, and fail-fast prerequisites"
    - path: tests/test_workflow_contract_docs.py
      provides: "Regression coverage for the published workflow contract language"
  key_links:
    - from: docs/design.md
      to: skill_pipeline/cli.py
      via: "documented deterministic command inventory"
      pattern: "raw-fetch|preprocess-source|canonicalize|check|verify|coverage|enrich-core|deal-agent"
    - from: docs/design.md
      to: skill_pipeline/paths.py
      via: "documented artifact roots and output paths"
      pattern: "raw/<slug>|data/deals/<slug>/source|data/skill/<slug>"
---

<objective>
Publish the contributor-facing workflow contract for the active brownfield pipeline.

Purpose: Make the deterministic vs LLM/skill-driven mix explicit in the repo's public design surface so contributors can orient without code archaeology.
Output: A rewritten `docs/design.md` plus a focused doc-regression test.
</objective>

<execution_context>
@/home/austinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/austinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
@docs/design.md
@skill_pipeline/cli.py
@skill_pipeline/paths.py
@tests/test_benchmark_separation_policy.py

<interfaces>
From `skill_pipeline/cli.py`:
```python
subcommands = [
    "deal-agent",
    "raw-fetch",
    "preprocess-source",
    "source-discover",
    "check",
    "verify",
    "coverage",
    "enrich-core",
    "canonicalize",
]
```

From `skill_pipeline/paths.py`:
```python
def build_skill_paths(deal_slug: str, *, project_root: Path = PROJECT_ROOT) -> SkillPathSet:
    ...
    source_dir = deals_root / deal_slug / "source"
    extract_dir = skill_root / "extract"
    check_dir = skill_root / "check"
    verify_dir = skill_root / "verify"
    coverage_dir = skill_root / "coverage"
    enrich_dir = skill_root / "enrich"
    export_dir = skill_root / "export"
    canonicalize_dir = skill_root / "canonicalize"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the design index as the public workflow contract</name>
  <files>docs/design.md</files>
  <read_first>
docs/design.md
.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
skill_pipeline/cli.py
skill_pipeline/paths.py
CLAUDE.md
  </read_first>
  <action>Rewrite `docs/design.md` into the contributor-facing brownfield contract for the active pipeline. Make the deterministic vs LLM/skill-driven mix explicit in a stage table that covers `skill-pipeline raw-fetch` through optional `/reconcile-alex`. For every active stage, document the entrypoint, stage type (`deterministic CLI`, `LLM skill`, `hybrid repair`, `formatting-only`, or `post-export diagnostic`), required upstream artifacts, primary outputs, and fail-fast prerequisite or boundary. Keep `skill-pipeline deal-agent` labeled as preflight/summary only, not the end-to-end runner. Preserve the benchmark-separation boundary and keep historical v3 notes clearly secondary.</action>
  <acceptance_criteria>
- `docs/design.md` names every active stage from `skill-pipeline raw-fetch` through `/reconcile-alex`.
- The document contains explicit deterministic vs LLM/skill-driven classification for each stage.
- The artifact roots `raw/<slug>/`, `data/deals/<slug>/source/`, and `data/skill/<slug>/...` are called out with their roles.
- `skill-pipeline deal-agent` is described as preflight/summary only.
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path("docs/design.md").read_text(encoding="utf-8")
required = [
    "skill-pipeline raw-fetch",
    "skill-pipeline preprocess-source",
    "/extract-deal <slug>",
    "skill-pipeline canonicalize",
    "skill-pipeline check",
    "skill-pipeline verify",
    "skill-pipeline coverage",
    "/verify-extraction <slug>",
    "skill-pipeline enrich-core",
    "/enrich-deal <slug>",
    "/export-csv <slug>",
    "/reconcile-alex <slug>",
    "deterministic",
    "LLM",
    "deal-agent",
    "raw/<slug>/",
    "data/deals/<slug>/source/",
    "data/skill/<slug>/",
]
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit(f"Missing workflow contract terms: {missing}")
PY</automated>
  </verify>
  <done>The top-level design doc works as a standalone workflow contract and does not require contributors to open code just to learn stage order, stage type, or artifact roots.</done>
</task>

<task type="auto">
  <name>Task 2: Add a focused workflow-doc regression test</name>
  <files>tests/test_workflow_contract_docs.py</files>
  <read_first>
tests/test_benchmark_separation_policy.py
docs/design.md
skill_pipeline/cli.py
skill_pipeline/paths.py
  </read_first>
  <action>Add `tests/test_workflow_contract_docs.py` to guard the published workflow contract in `docs/design.md`. Assert the documented stage order, deterministic vs LLM/hybrid/formatting distinctions, artifact-root names, and the `deal-agent` summary-only limitation. Keep the assertions specific enough to catch contract drift without making harmless prose edits fail.</action>
  <acceptance_criteria>
- `tests/test_workflow_contract_docs.py` exists with clear test names tied to workflow contract expectations.
- The test checks explicit deterministic vs LLM/hybrid/formatting labels instead of a single generic keyword.
- The test passes against the rewritten `docs/design.md`.
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_workflow_contract_docs.py</automated>
  </verify>
  <done>The contributor-facing workflow contract has automated regression coverage that fails if stage order, stage-type labels, or artifact-root language drifts.</done>
</task>

</tasks>

<verification>
Run `pytest -q tests/test_workflow_contract_docs.py`.
</verification>

<success_criteria>
- `docs/design.md` is the clear public index for the active hybrid workflow.
- The deterministic vs LLM/skill-driven split is explicit in the document itself.
- A focused pytest module guards the published contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-contract-surface/01-01-SUMMARY.md`
</output>

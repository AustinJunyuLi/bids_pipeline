---
phase: 01-workflow-contract-surface
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/skills/README.md
  - .codex/skills/README.md
  - .cursor/skills/README.md
  - tests/test_benchmark_separation_policy.py
autonomous: true
requirements:
  - WFLO-01
must_haves:
  truths:
    - "A contributor reading `CLAUDE.md` can see the deterministic CLI stages, LLM skills, hybrid repair stage, and post-export diagnostic boundary without ambiguity."
    - "A contributor reading the skill index does not mistake `deal-agent` for a single end-to-end runner that skips deterministic gates."
  artifacts:
    - path: CLAUDE.md
      provides: "Authoritative repo instructions with explicit hybrid workflow split"
    - path: .claude/skills/README.md
      provides: "Canonical skills index that reflects the actual deterministic vs skill-driven sequence"
    - path: tests/test_benchmark_separation_policy.py
      provides: "Regression checks that instruction docs preserve the hybrid contract and benchmark boundary"
  key_links:
    - from: .claude/skills/README.md
      to: scripts/sync_skill_mirrors.py
      via: "canonical-to-mirror synchronization"
      pattern: "sync_skill_mirrors.py"
    - from: tests/test_benchmark_separation_policy.py
      to: CLAUDE.md
      via: "instruction-surface assertions"
      pattern: "benchmark|deal-agent|deterministic|LLM"
---

<objective>
Align the authoritative repo instructions and the canonical skill index with the actual hybrid pipeline.

Purpose: Make the deterministic vs LLM/skill-driven mix explicit anywhere a contributor is likely to start, not just in the design doc.
Output: Updated instruction surfaces, synced skill readme mirrors, and strengthened policy tests.
</objective>

<execution_context>
@/home/austinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/austinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
@CLAUDE.md
@.claude/skills/README.md
@.claude/skills/deal-agent/SKILL.md
@skill_pipeline/cli.py
@skill_pipeline/paths.py
@scripts/sync_skill_mirrors.py
@tests/test_benchmark_separation_policy.py
@tests/test_skill_mirror_sync.py

<interfaces>
Current canonical skills index:
```markdown
| `deal-agent` | Orchestrator - runs all 4 skills in sequence |
| `reconcile-alex` | Post-export benchmark QA against Alex's spreadsheet |
```

Current mirror policy:
```python
CANONICAL_RELATIVE = Path(".claude/skills")
MIRROR_RELATIVES = (Path(".codex/skills"), Path(".cursor/skills"))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `CLAUDE.md` state the hybrid workflow split explicitly</name>
  <files>CLAUDE.md, tests/test_benchmark_separation_policy.py</files>
  <read_first>
CLAUDE.md
.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
skill_pipeline/cli.py
skill_pipeline/paths.py
tests/test_benchmark_separation_policy.py
  </read_first>
  <action>Update `CLAUDE.md` so the authoritative repo instructions clearly enumerate the deterministic CLI stages, the LLM skill stages, the hybrid `/verify-extraction` repair loop, the formatting-only `/export-csv` stage, and the post-export `/reconcile-alex` diagnostic boundary. Call out `skill-pipeline deal-agent` as preflight/summary only, and point contributors to `docs/design.md` as the public workflow contract. Extend `tests/test_benchmark_separation_policy.py` with assertions that the instruction surface keeps this hybrid split and `deal-agent` limitation explicit while preserving the existing benchmark-boundary guarantees.</action>
  <acceptance_criteria>
- `CLAUDE.md` contains an explicit hybrid stage taxonomy instead of only an implied sequence.
- `CLAUDE.md` describes `skill-pipeline deal-agent` as preflight/summary only.
- `tests/test_benchmark_separation_policy.py` checks the hybrid workflow wording without weakening existing benchmark-boundary assertions.
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path("CLAUDE.md").read_text(encoding="utf-8")
required = [
    "deterministic",
    "LLM",
    "/verify-extraction",
    "/export-csv",
    "/reconcile-alex",
    "preflight / summary",
    "docs/design.md",
]
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit(f"Missing CLAUDE workflow terms: {missing}")
PY</automated>
  </verify>
  <done>The authoritative instruction file exposes the deterministic vs LLM/skill-driven split directly and existing policy tests are extended to guard it.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the canonical skills index and sync derived mirrors</name>
  <files>.claude/skills/README.md, .codex/skills/README.md, .cursor/skills/README.md</files>
  <read_first>
.claude/skills/README.md
.claude/skills/deal-agent/SKILL.md
scripts/sync_skill_mirrors.py
tests/test_skill_mirror_sync.py
tests/test_benchmark_separation_policy.py
  </read_first>
  <action>Rewrite `.claude/skills/README.md` so it describes the actual hybrid sequence: deterministic CLI stages wrapped around LLM skills, `deal-agent` as a thin orchestrator that still depends on deterministic gates, and `/reconcile-alex` as post-export only. Do not edit `.codex/skills/README.md` or `.cursor/skills/README.md` by hand; run `python scripts/sync_skill_mirrors.py` so the canonical README propagates into both mirrors with LF normalization and no drift.</action>
  <acceptance_criteria>
- `.claude/skills/README.md` makes the deterministic vs skill-driven mix explicit.
- The README no longer implies that `deal-agent` only runs four skills end to end.
- `.codex/skills/README.md` and `.cursor/skills/README.md` match the canonical README after sync.
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py</automated>
  </verify>
  <done>The repo's first-stop instruction surfaces agree on the hybrid workflow and mirror-sync policy tests pass.</done>
</task>

</tasks>

<verification>
Run `pytest -q tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py`.
</verification>

<success_criteria>
- `CLAUDE.md` and the canonical skill index both make the deterministic vs LLM/skill-driven mix explicit.
- `deal-agent` is no longer described in a way that hides deterministic gates.
- Skill readme mirrors stay synced from `.claude/skills/`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-contract-surface/01-02-SUMMARY.md`
</output>

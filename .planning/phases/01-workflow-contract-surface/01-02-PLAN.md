---
phase: 01-workflow-contract-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - CLAUDE.md
  - docs/HOME_COMPUTER_SETUP.md
  - .claude/skills/README.md
  - .claude/skills/deal-agent/SKILL.md
  - .claude/skills/extract-deal/SKILL.md
  - .claude/skills/verify-extraction/SKILL.md
  - .claude/skills/enrich-deal/SKILL.md
  - .claude/skills/export-csv/SKILL.md
  - .codex/skills/
  - .cursor/skills/
  - tests/test_benchmark_separation_policy.py
  - tests/test_skill_mirror_sync.py
autonomous: true
requirements:
  - WFLO-01
user_setup: []
must_haves:
  truths:
    - "Authoritative entrypoint docs describe the same live workflow contract as docs/workflow-contract.md."
    - "Canonical skill docs no longer claim artifact reads or stage roles that conflict with the current deterministic runtime."
    - "Mirror skill trees stay synchronized after canonical `.claude/skills` edits."
  artifacts:
    - path: CLAUDE.md
      provides: "Authoritative repo instruction file aligned with the live stage contract"
      min_lines: 120
      contains: "skill-pipeline deal-agent"
    - path: .claude/skills/deal-agent/SKILL.md
      provides: "Canonical orchestrator skill contract aligned with deterministic handoff stages"
      min_lines: 80
      contains: "Deterministic runtime stages"
    - path: .claude/skills/extract-deal/SKILL.md
      provides: "Canonical extraction skill doc with accurate read/write contract"
      min_lines: 150
      contains: "chronology_blocks.jsonl"
  key_links:
    - from: CLAUDE.md
      to: docs/workflow-contract.md
      via: "matching stage order and role descriptions"
      pattern: "verify-extraction"
    - from: .claude/skills/
      to: .codex/skills/
      via: "scripts/sync_skill_mirrors.py mirror sync"
      pattern: "SKILL\\.md"
    - from: tests/test_benchmark_separation_policy.py
      to: .claude/skills/
      via: "policy assertions over canonical generation docs"
      pattern: "PROJECT_ROOT / \".claude/skills"
---

<objective>
Reconcile the authoritative docs and canonical skill contracts with the live artifact paths, entrypoints, and fail-fast boundaries.

Purpose: The current brownfield risk is documentation drift, not missing runtime code. Phase 1 needs the committed docs to say the same thing as the actual pipeline.
Output: Updated top-level instructions, corrected canonical skill docs, synchronized mirror trees, and doc-policy tests that still pass after the contract update.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-contract-surface/01-RESEARCH.md
@.planning/phases/01-workflow-contract-surface/01-01-PLAN.md
@docs/workflow-contract.md
@CLAUDE.md
@docs/HOME_COMPUTER_SETUP.md
@.claude/skills/README.md
@.claude/skills/deal-agent/SKILL.md
@.claude/skills/extract-deal/SKILL.md
@.claude/skills/verify-extraction/SKILL.md
@.claude/skills/enrich-deal/SKILL.md
@.claude/skills/export-csv/SKILL.md
@tests/test_benchmark_separation_policy.py
@tests/test_skill_mirror_sync.py
@scripts/sync_skill_mirrors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align top-level instructions and contributor docs with the contract surface</name>
  <files>CLAUDE.md, docs/HOME_COMPUTER_SETUP.md</files>
  <read_first>CLAUDE.md, docs/HOME_COMPUTER_SETUP.md, docs/workflow-contract.md, docs/design.md, skill_pipeline/cli.py, skill_pipeline/deal_agent.py</read_first>
  <action>Update `CLAUDE.md` and `docs/HOME_COMPUTER_SETUP.md` so they match the committed contract doc exactly. Preserve `CLAUDE.md` as the authoritative repo instruction file, but ensure it states the current stage order, clarifies that `skill-pipeline deal-agent` is preflight/summary only while `/deal-agent` is the orchestrating skill, and removes or corrects any artifact-path claims that no longer match the live runtime. Keep the existing benchmark-separation language intact while making the deterministic-versus-skill split easier to recover from a single read.</action>
  <verify>rg -n "skill-pipeline deal-agent|/deal-agent|/verify-extraction|benchmark" CLAUDE.md docs/HOME_COMPUTER_SETUP.md</verify>
  <acceptance_criteria>
    - CLAUDE.md contains both "skill-pipeline deal-agent" and "/deal-agent"
    - CLAUDE.md still contains benchmark-separation language with "before `/export-csv`"
    - docs/HOME_COMPUTER_SETUP.md matches the updated contributor-facing entrypoint guidance
  </acceptance_criteria>
  <done>The authoritative repo docs describe the same live contract as the new workflow contract doc.</done>
</task>

<task type="auto">
  <name>Task 2: Correct canonical skill docs for live artifact paths and stage roles</name>
  <files>.claude/skills/README.md, .claude/skills/deal-agent/SKILL.md, .claude/skills/extract-deal/SKILL.md, .claude/skills/verify-extraction/SKILL.md, .claude/skills/enrich-deal/SKILL.md, .claude/skills/export-csv/SKILL.md</files>
  <read_first>.claude/skills/README.md, .claude/skills/deal-agent/SKILL.md, .claude/skills/extract-deal/SKILL.md, .claude/skills/verify-extraction/SKILL.md, .claude/skills/enrich-deal/SKILL.md, .claude/skills/export-csv/SKILL.md, docs/workflow-contract.md, skill_pipeline/cli.py, skill_pipeline/deal_agent.py, skill_pipeline/preprocess/source.py</read_first>
  <action>Update the canonical `.claude/skills` docs so their Reads, Writes, and stage-role sections match the live runtime. Explicitly preserve the benchmark boundary, but correct stale claims discovered in research: document the `deal-agent` distinction clearly, ensure each skill's prerequisite and handoff text matches the deterministic stages around it, and remove or qualify artifact claims that conflict with the live preprocess contract, especially any unconditional dependence on `supplementary_snippets.jsonl` when the current preprocess stage deletes stale snippet output. Keep the skill docs focused on the current brownfield runtime, not historical rewrite plans.</action>
  <verify>rg -n "supplementary_snippets|skill-pipeline deal-agent|/deal-agent|deterministic" .claude/skills/README.md .claude/skills/deal-agent/SKILL.md .claude/skills/extract-deal/SKILL.md .claude/skills/verify-extraction/SKILL.md .claude/skills/enrich-deal/SKILL.md .claude/skills/export-csv/SKILL.md</verify>
  <acceptance_criteria>
    - .claude/skills/deal-agent/SKILL.md distinguishes the CLI summary command from the orchestrating skill
    - .claude/skills/extract-deal/SKILL.md and .claude/skills/enrich-deal/SKILL.md do not present stale source-artifact requirements as live hard prerequisites
    - All updated `.claude/skills` docs retain benchmark-boundary language with "before `/export-csv`"
  </acceptance_criteria>
  <done>The canonical skill docs now match the live runtime and its fail-fast boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Sync mirror skill trees and re-run doc-policy checks</name>
  <files>.codex/skills/, .cursor/skills/, tests/test_benchmark_separation_policy.py, tests/test_skill_mirror_sync.py</files>
  <read_first>scripts/sync_skill_mirrors.py, tests/test_benchmark_separation_policy.py, tests/test_skill_mirror_sync.py, .claude/skills/README.md</read_first>
  <action>Run `python3 scripts/sync_skill_mirrors.py` so `.codex/skills/` and `.cursor/skills/` reflect the canonical `.claude/skills` edits. Update `tests/test_benchmark_separation_policy.py` only if the contract wording changes require the assertions to reference the new canonical phrasing without weakening the benchmark boundary. Keep `tests/test_skill_mirror_sync.py` passing against the synchronized mirror trees.</action>
  <verify>python3 scripts/sync_skill_mirrors.py --check && pytest -q tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py</verify>
  <acceptance_criteria>
    - `python3 scripts/sync_skill_mirrors.py --check` exits 0
    - `pytest -q tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py` exits 0
    - `.codex/skills/` and `.cursor/skills/` reflect the updated `.claude/skills` files
  </acceptance_criteria>
  <done>The canonical skill contract and both mirror trees are synchronized and protected by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 scripts/sync_skill_mirrors.py --check` passes
- [ ] `pytest -q tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py` passes
- [ ] `rg -n "skill-pipeline deal-agent|/deal-agent" CLAUDE.md .claude/skills/deal-agent/SKILL.md` returns matches
</verification>

<success_criteria>

- All tasks completed
- Authoritative docs and canonical skill docs match the live runtime contract
- Mirror skill trees are synchronized from `.claude/skills`
- Benchmark-boundary and mirror-sync tests still protect the corrected contract
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-contract-surface/01-02-SUMMARY.md`
</output>

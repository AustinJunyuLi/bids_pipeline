---
phase: 20-proposal-linkage-bid-type-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/derive.py
  - skill_pipeline/gates_v2.py
  - tests/test_skill_derive.py
  - tests/test_skill_gates_v2.py
autonomous: true
requirements: [LINK-01, LINK-02, LINK-03]

must_haves:
  truths:
    - "proposal derivation no longer trusts forward or non-solicitation `requested_by_observation_id` links"
    - "proposal `bid_type` is determined from proposal-local formality cues before falling back to phase kind"
    - "`gates-v2` blocks malformed proposal-request links before derive"
    - "focused regression tests pin the chronology and bid-type precedence rules"
  artifacts:
    - path: "skill_pipeline/derive.py"
      provides: "chronology-safe proposal phase selection and bid-type inference"
    - path: "skill_pipeline/gates_v2.py"
      provides: "proposal link chronology/type blockers"
    - path: "tests/test_skill_derive.py"
      provides: "proposal-link fallback and bid-type regression coverage"
    - path: "tests/test_skill_gates_v2.py"
      provides: "proposal link blocker regression coverage"
  key_links:
    - from: "skill_pipeline/derive.py"
      to: "tests/test_skill_derive.py"
      via: "phase fallback and signal-precedence fixtures"
      pattern: "derive remains safe even when older artifacts contain bad links"
    - from: "skill_pipeline/gates_v2.py"
      to: "tests/test_skill_gates_v2.py"
      via: "forward-link and wrong-type fixtures"
      pattern: "malformed proposal links fail before downstream derive/export"
---

<objective>
Repair proposal chronology and bid-type logic.

Purpose: GPT Pro found that late-stage proposals are misclassified because
derive inherits `bid_type` from invalid or overly coarse phase links. This plan
repairs the phase association, adds bounded proposal-local signal inference,
and blocks malformed links at the semantic-gate layer.

Output: safe proposal phase resolution, improved proposal bid-type inference,
and focused test coverage for the new rules.
</objective>

<context>
@.planning/phases/20-proposal-linkage-bid-type-repair/20-CONTEXT.md
@skill_pipeline/derive.py
@skill_pipeline/gates_v2.py
@tests/_v2_validation_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make derive chronology-safe for proposal association and bid-type</name>
  <files>
    skill_pipeline/derive.py
  </files>
  <action>
Add a proposal-specific phase resolver that:
- trusts `requested_by_observation_id` only when it targets a solicitation that
  occurs on or before the proposal date
- otherwise falls back to the latest valid same-day-or-earlier solicitation

Then update proposal row compilation to:
- infer `bid_type` from proposal-local formal/informal cues first
- fall back to repaired phase kind only when local cues stay inconclusive
- keep cash-regime grouping aligned with the repaired proposal-phase mapping
  </action>
  <acceptance_criteria>
    - future-linked proposals do not inherit a later phase
    - wrong-type links do not influence proposal derivation
    - strong proposal-local formality cues can produce `Formal` even when the
      phase is missing or informal
    - explicit non-binding language keeps mixed-signal proposals conservative
  </acceptance_criteria>
  <done>proposal rows are no longer driven by malformed links or phase-only inheritance</done>
</task>

<task type="auto">
  <name>Task 2: Block malformed proposal links and pin the rules with tests</name>
  <files>
    skill_pipeline/gates_v2.py,
    tests/test_skill_derive.py,
    tests/test_skill_gates_v2.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_derive.py tests/test_skill_gates_v2.py -k 'proposal or link or bid_type'</automated>
  </verify>
  <action>
Extend `gates_v2.py` with explicit findings for:
- proposal links that point to a non-solicitation observation
- proposal links that point forward in time

Add synthetic regression tests that cover:
- future-link fallback in derive
- proposal-local formality cues outranking missing or invalid phase context
- gate blockers for both chronology and wrong-type proposal links
  </action>
  <acceptance_criteria>
    - malformed proposal-request links fail in `gates-v2`
    - derive remains deterministic on older artifacts by ignoring invalid links
    - focused pytest coverage documents the precedence order
  </acceptance_criteria>
  <done>proposal chronology and bid-type behavior are locked by direct tests</done>
</task>

</tasks>

---
phase: 02-prompt-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/pipeline_models/prompt.py
  - skill_pipeline/pipeline_models/__init__.py
  - skill_pipeline/models.py
  - skill_pipeline/paths.py
  - skill_pipeline/cli.py
  - skill_pipeline/compose_prompts.py
  - tests/test_skill_prompt_models.py
  - tests/test_skill_pipeline.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-05
user_setup: []
must_haves:
  truths:
    - "`skill-pipeline compose-prompts` exists as a deterministic runtime stage and performs no model calls"
    - "prompt packet artifacts live under `data/skill/<slug>/prompt/` instead of being mixed into `extract/`"
    - "prompt packet manifests and paths are schema-validated rather than untyped dicts"
  artifacts:
    - path: "skill_pipeline/pipeline_models/prompt.py"
      provides: "prompt packet artifact schema"
      exports: ["PromptPacketManifest"]
    - path: "skill_pipeline/compose_prompts.py"
      provides: "deterministic prompt composition stage entrypoint"
      contains: "run_compose_prompts"
    - path: "skill_pipeline/cli.py"
      provides: "compose-prompts CLI subcommand"
      contains: "compose-prompts"
    - path: "tests/test_skill_prompt_models.py"
      provides: "schema and path contract regression coverage"
      min_lines: 40
  key_links:
    - from: "skill_pipeline/cli.py"
      to: "skill_pipeline/compose_prompts.py"
      via: "compose-prompts subcommand dispatch"
      pattern: "run_compose_prompts"
    - from: "skill_pipeline/paths.py"
      to: "skill_pipeline/models.py"
      via: "SkillPathSet prompt paths"
      pattern: "prompt_"
---

<objective>
Create the deterministic prompt-packet stage contract, artifact paths, and CLI
entrypoint for Phase 2.

Purpose: later rendering and skill integration work depend on a persisted,
provider-neutral prompt artifact layer that sits between `preprocess-source`
and `/extract-deal`.
Output: prompt packet models, prompt output paths, and a `compose-prompts`
runtime stage shell with contract tests.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-architecture/02-CONTEXT.md
@.planning/phases/02-prompt-architecture/02-RESEARCH.md
@.planning/phases/02-prompt-architecture/02-VALIDATION.md
@skill_pipeline/cli.py
@skill_pipeline/models.py
@skill_pipeline/paths.py
@skill_pipeline/pipeline_models/source.py
@.claude/skills/extract-deal/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define schema-validated prompt packet models</name>
  <files>skill_pipeline/pipeline_models/prompt.py, skill_pipeline/pipeline_models/__init__.py</files>
  <read_first>skill_pipeline/pipeline_models/source.py, skill_pipeline/pipeline_models/common.py, skill_pipeline/pipeline_models/__init__.py, .planning/phases/02-prompt-architecture/02-CONTEXT.md, .planning/phases/02-prompt-architecture/02-RESEARCH.md</read_first>
  <action>Create `skill_pipeline/pipeline_models/prompt.py` with concrete provider-neutral models for prompt artifacts. Include `PromptChunkWindow` with `window_id`, `chunk_index`, `chunk_count`, `target_block_ids`, `overlap_block_ids`, and `estimated_tokens`; `PromptPacketArtifact` with `packet_id`, `packet_family: Literal["actors", "events"]`, `chunk_mode: Literal["single_pass", "chunked"]`, `window_id`, `prefix_path`, `body_path`, `rendered_path`, `evidence_ids`, and `actor_roster_source_path`; and `PromptPacketManifest` with `deal_slug`, `source_accession_number`, `packets`, `asset_files`, and `notes`. Export these models from `skill_pipeline/pipeline_models/__init__.py`. Do not add provider names, chat-role fields, or model-call payload shapes.</action>
  <verify>python -m pytest -q tests/test_skill_prompt_models.py</verify>
  <acceptance_criteria>
    - `skill_pipeline/pipeline_models/prompt.py` contains `class PromptChunkWindow`
    - `skill_pipeline/pipeline_models/prompt.py` contains `packet_family: Literal["actors", "events"]`
    - `skill_pipeline/pipeline_models/prompt.py` contains `chunk_mode: Literal["single_pass", "chunked"]`
    - `skill_pipeline/pipeline_models/__init__.py` exports `PromptPacketManifest`
  </acceptance_criteria>
  <done>Prompt artifact schemas exist and can validate persisted packet manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend path contracts and expose the compose-prompts runtime stage</name>
  <files>skill_pipeline/models.py, skill_pipeline/paths.py, skill_pipeline/cli.py, skill_pipeline/compose_prompts.py</files>
  <read_first>skill_pipeline/models.py, skill_pipeline/paths.py, skill_pipeline/cli.py, skill_pipeline/preprocess/source.py, .planning/phases/02-prompt-architecture/02-CONTEXT.md</read_first>
  <action>Extend `SkillPathSet` with concrete prompt-stage paths: `prompt_dir`, `prompt_packets_dir`, and `prompt_manifest_path`. Update `build_skill_paths(...)` and `ensure_output_directories(...)` so those directories are created alongside the existing deterministic stage directories. Add a new `compose-prompts` subcommand to `skill_pipeline/cli.py` with `--deal`, `--mode` choices `actors|events|all`, and `--chunk-budget` integer default `6000`. Create `skill_pipeline/compose_prompts.py` with `run_compose_prompts(deal_slug, project_root, mode="all", chunk_budget=6000)` that loads the required source artifacts, validates their presence, creates the prompt output directories, and writes a schema-valid manifest stub. The stage must stop before any model invocation and must not import provider SDKs.</action>
  <verify>python -m pytest -q tests/test_skill_pipeline.py -k "compose_prompts or prompt_paths"</verify>
  <acceptance_criteria>
    - `skill_pipeline/models.py` contains `prompt_dir`, `prompt_packets_dir`, and `prompt_manifest_path`
    - `skill_pipeline/paths.py` contains `prompt_dir=skill_root / "prompt"`
    - `skill_pipeline/cli.py` contains `compose-prompts`
    - `skill_pipeline/compose_prompts.py` contains `def run_compose_prompts(`
  </acceptance_criteria>
  <done>The runtime knows where prompt artifacts live and exposes a deterministic stage entrypoint for building them.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract-level tests for prompt models, paths, and CLI failures</name>
  <files>tests/test_skill_prompt_models.py, tests/test_skill_pipeline.py</files>
  <read_first>tests/test_skill_pipeline.py, skill_pipeline/pipeline_models/prompt.py, skill_pipeline/compose_prompts.py, skill_pipeline/paths.py, .planning/phases/02-prompt-architecture/02-VALIDATION.md</read_first>
  <action>Create `tests/test_skill_prompt_models.py` with schema tests that validate `PromptChunkWindow`, `PromptPacketArtifact`, and `PromptPacketManifest`. Extend `tests/test_skill_pipeline.py` so a prompt-stage fixture asserts `build_skill_paths()` exposes the three prompt paths, `ensure_output_directories()` creates the prompt directories, and `compose-prompts` fails cleanly when `chronology_blocks.jsonl` or `evidence_items.jsonl` are missing. The tests should use temporary directories and must not rely on any live network or model call.</action>
  <verify>python -m pytest -q tests/test_skill_prompt_models.py tests/test_skill_pipeline.py</verify>
  <acceptance_criteria>
    - `tests/test_skill_prompt_models.py` exists
    - `tests/test_skill_prompt_models.py` contains `PromptPacketManifest`
    - `tests/test_skill_pipeline.py` contains `compose-prompts`
    - `tests/test_skill_pipeline.py` asserts prompt directories are created or missing inputs raise
  </acceptance_criteria>
  <done>The prompt stage contract is protected by deterministic tests before packet rendering begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_skill_prompt_models.py` passes
- [ ] `python -m pytest -q tests/test_skill_pipeline.py -k "compose_prompts or prompt_paths"` passes
- [ ] `skill_pipeline/compose_prompts.py` contains no provider SDK imports
</verification>

<success_criteria>

- All tasks completed
- Prompt packet artifacts have a typed schema and stable paths
- `compose-prompts` exists as a deterministic CLI stage shell
- Contract tests enforce the new prompt-stage boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-architecture/02-01-SUMMARY.md`
</output>

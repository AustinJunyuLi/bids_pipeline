---
phase: 02-prompt-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - scripts/validate_prompt_packets.py
  - skill_pipeline/deal_agent.py
  - CLAUDE.md
  - docs/design.md
  - .claude/skills/extract-deal/SKILL.md
  - .codex/skills/extract-deal/SKILL.md
  - .cursor/skills/extract-deal/SKILL.md
  - tests/test_skill_pipeline.py
  - tests/test_skill_mirror_sync.py
  - tests/test_runtime_contract_docs.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-05
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
user_setup: []
must_haves:
  truths:
    - "the live extraction skill reads composed prompt packet artifacts instead of mentally assembling prompts from raw source inputs"
    - "prompt packet artifacts can be validated deterministically on `stec` before an LLM extraction run"
    - "runtime docs and deal-agent status expose the prompt stage without introducing provider-specific Python behavior"
  artifacts:
    - path: "scripts/validate_prompt_packets.py"
      provides: "deterministic validator for prompt packet artifacts"
      exports: ["main"]
    - path: ".claude/skills/extract-deal/SKILL.md"
      provides: "canonical extract workflow contract for prompt packet consumption"
      contains: "data/skill/<slug>/prompt/"
    - path: "docs/design.md"
      provides: "updated runtime sequence including compose-prompts"
      contains: "compose-prompts"
  key_links:
    - from: ".claude/skills/extract-deal/SKILL.md"
      to: "scripts/validate_prompt_packets.py"
      via: "prompt packet preflight and usage contract"
      pattern: "prompt"
    - from: "tests/test_skill_mirror_sync.py"
      to: ".claude/skills/extract-deal/SKILL.md"
      via: "mirror synchronization assertions"
      pattern: "extract-deal"
---

<objective>
Integrate the prompt packet stage into the live extraction workflow and prove
the new artifacts work on `stec`.

Purpose: Phase 2 is incomplete until the canonical extract skill, runtime docs,
and deterministic validation flow all acknowledge the new prompt stage.
Output: prompt packet validator, updated extract skill contract and mirrors,
deal-agent/status visibility, and `stec` prompt packet validation.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-architecture/02-CONTEXT.md
@.planning/phases/02-prompt-architecture/02-RESEARCH.md
@.planning/phases/02-prompt-architecture/02-VALIDATION.md
@CLAUDE.md
@docs/design.md
@.claude/skills/extract-deal/SKILL.md
@skill_pipeline/deal_agent.py
@skill_pipeline/compose_prompts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prompt packet validation and surface prompt-stage status in deal-agent</name>
  <files>scripts/validate_prompt_packets.py, skill_pipeline/deal_agent.py, tests/test_skill_pipeline.py</files>
  <read_first>skill_pipeline/deal_agent.py, skill_pipeline/compose_prompts.py, skill_pipeline/pipeline_models/prompt.py, .planning/phases/02-prompt-architecture/02-VALIDATION.md, .planning/phases/02-prompt-architecture/02-CONTEXT.md</read_first>
  <action>Create `scripts/validate_prompt_packets.py` with `--deal`, `--project-root`, and `--expect-sections` flags. The script must load `data/skill/<slug>/prompt/manifest.json`, validate it through `PromptPacketManifest`, open every `rendered.md` file named in the manifest, and assert the required tags `<chronology_blocks>`, `<evidence_checklist>`, and `<task_instructions>` are present; if a packet is `chunked`, it must also contain `<overlap_context>`. Extend `skill_pipeline/deal_agent.py` so the summary reports prompt-stage status from prompt-manifest existence plus packet counts, without invoking `compose-prompts` itself. Add `tests/test_skill_pipeline.py` coverage for the new prompt-stage status fields.</action>
  <verify>python -m pytest -q tests/test_skill_pipeline.py -k "prompt_stage or validate_prompt_packets"</verify>
  <acceptance_criteria>
    - `scripts/validate_prompt_packets.py` exists
    - `scripts/validate_prompt_packets.py` contains `--expect-sections`
    - `skill_pipeline/deal_agent.py` contains prompt-stage status or packet-count reporting
    - `tests/test_skill_pipeline.py` asserts prompt-stage status in the deal-agent summary
  </acceptance_criteria>
  <done>The runtime can preflight prompt artifacts deterministically before any extraction run uses them.</done>
</task>

<task type="auto">
  <name>Task 2: Update the canonical extract skill and sync all skill mirrors</name>
  <files>CLAUDE.md, docs/design.md, .claude/skills/extract-deal/SKILL.md, .codex/skills/extract-deal/SKILL.md, .cursor/skills/extract-deal/SKILL.md, tests/test_skill_mirror_sync.py, tests/test_runtime_contract_docs.py</files>
  <read_first>CLAUDE.md, docs/design.md, .claude/skills/extract-deal/SKILL.md, .planning/phases/02-prompt-architecture/02-CONTEXT.md, .planning/phases/02-prompt-architecture/02-RESEARCH.md</read_first>
  <action>Update `CLAUDE.md` and `docs/design.md` so the live deterministic runtime sequence becomes `preprocess-source -> compose-prompts -> /extract-deal`. Update `.claude/skills/extract-deal/SKILL.md` so its `Reads` table explicitly includes `data/skill/<slug>/prompt/manifest.json` and packet files, and its extraction method instructs the agent to use the composed actor and event prompt packets instead of manually assembling prompts from raw source artifacts. Sync the same content into `.codex/skills/extract-deal/SKILL.md` and `.cursor/skills/extract-deal/SKILL.md`. Extend `tests/test_skill_mirror_sync.py` and `tests/test_runtime_contract_docs.py` so mirror sync and live-doc assertions fail if the prompt stage disappears from the canonical docs again.</action>
  <verify>python -m pytest -q tests/test_skill_mirror_sync.py tests/test_runtime_contract_docs.py</verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains `compose-prompts`
    - `docs/design.md` contains `compose-prompts`
    - `.claude/skills/extract-deal/SKILL.md` contains `data/skill/<slug>/prompt/manifest.json`
    - `.codex/skills/extract-deal/SKILL.md` and `.cursor/skills/extract-deal/SKILL.md` match the canonical `.claude` skill
  </acceptance_criteria>
  <done>The prompt stage becomes part of the live repo contract and all skill mirrors stay aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Validate `stec` prompt packets and record the extraction-quality check path</name>
  <files>scripts/validate_prompt_packets.py, tests/test_skill_pipeline.py, tests/test_runtime_contract_docs.py</files>
  <read_first>scripts/validate_prompt_packets.py, skill_pipeline/compose_prompts.py, .claude/skills/extract-deal/SKILL.md, data/deals/stec/source/chronology_blocks.jsonl, .planning/phases/02-prompt-architecture/02-VALIDATION.md</read_first>
  <action>Use the new stage to generate `stec` prompt packets with `skill-pipeline compose-prompts --deal stec --mode all --chunk-budget 6000`, then validate them with `python scripts/validate_prompt_packets.py --deal stec --project-root /home/austinli/Projects/bids_pipeline --expect-sections`. Extend `tests/test_skill_pipeline.py` or `tests/test_runtime_contract_docs.py` so the repo has a deterministic regression asserting that `compose-prompts` is part of the runtime contract and that the packet validator can run on a temporary fixture manifest. In the eventual execution summary, record the exact `stec` prompt validation command and the follow-on extraction-quality comparison path: `/extract-deal stec` against the generated packets, followed by deterministic gates to compare with the pre-Phase-2 baseline.</action>
  <verify>python scripts/validate_prompt_packets.py --deal stec --project-root /home/austinli/Projects/bids_pipeline --expect-sections && python -m pytest -q tests/test_skill_pipeline.py tests/test_skill_mirror_sync.py tests/test_runtime_contract_docs.py</verify>
  <acceptance_criteria>
    - `scripts/validate_prompt_packets.py --help` mentions `--expect-sections`
    - `tests/test_skill_pipeline.py` or `tests/test_runtime_contract_docs.py` asserts the live runtime sequence includes `compose-prompts`
    - the plan summary instructions include the exact `stec` validation command shown in the task action
    - `stec` prompt packet validation is executable before an extraction-quality comparison run
  </acceptance_criteria>
  <done>The phase has a real-deal validation path and a documented route to the roadmap’s `stec` quality comparison exit criterion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_skill_pipeline.py -k "prompt_stage or validate_prompt_packets"` passes
- [ ] `python -m pytest -q tests/test_skill_mirror_sync.py tests/test_runtime_contract_docs.py` passes
- [ ] `python scripts/validate_prompt_packets.py --deal stec --project-root /home/austinli/Projects/bids_pipeline --expect-sections` passes
</verification>

<success_criteria>

- All tasks completed
- Prompt packets are part of the live extraction contract and visible in runtime docs
- `stec` prompt packets can be validated deterministically before extraction
- The repo has a documented path to the Phase 2 extraction-quality exit criterion
  </success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-architecture/02-03-SUMMARY.md`
</output>

---
phase: 02-prompt-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - skill_pipeline/compose_prompts.py
  - skill_pipeline/prompts/__init__.py
  - skill_pipeline/prompts/chunks.py
  - skill_pipeline/prompts/checklist.py
  - skill_pipeline/prompts/render.py
  - skill_pipeline/prompt_assets/actors_prefix.md
  - skill_pipeline/prompt_assets/events_prefix.md
  - skill_pipeline/prompt_assets/event_examples.md
  - tests/test_skill_compose_prompts.py
autonomous: true
requirements:
  - INFRA-03
  - INFRA-05
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
user_setup: []
must_haves:
  truths:
    - "rendered prompt packets place chronology blocks before task instructions"
    - "chunk windows never split chronology blocks and chunked packets use explicit 2-block `<overlap_context>` sections"
    - "evidence items are rendered as an active checklist rather than a passive appendix"
  artifacts:
    - path: "skill_pipeline/prompts/chunks.py"
      provides: "deterministic whole-block chunk planning"
      exports: ["build_chunk_windows"]
    - path: "skill_pipeline/prompts/render.py"
      provides: "provider-neutral packet rendering"
      exports: ["render_actor_packet", "render_event_packet"]
    - path: "tests/test_skill_compose_prompts.py"
      provides: "prompt chunking and rendering regression coverage"
      min_lines: 60
  key_links:
    - from: "skill_pipeline/compose_prompts.py"
      to: "skill_pipeline/prompts/chunks.py"
      via: "chunk window planning"
      pattern: "build_chunk_windows"
    - from: "skill_pipeline/compose_prompts.py"
      to: "skill_pipeline/prompts/render.py"
      via: "packet rendering"
      pattern: "render_"
---

<objective>
Implement the deterministic chunk planner, evidence checklist builder, and
packet renderer that turn annotated source artifacts into real prompt packets.

Purpose: Phase 2 is only useful if the new stage produces inspectable packets
that embody chronology-first ordering, whole-block chunking, and explicit
overlap semantics.
Output: prompt rendering helpers, file-backed stable assets, and packet
regression tests.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-architecture/02-CONTEXT.md
@.planning/phases/02-prompt-architecture/02-RESEARCH.md
@.planning/phases/02-prompt-architecture/02-VALIDATION.md
@skill_pipeline/compose_prompts.py
@skill_pipeline/pipeline_models/prompt.py
@skill_pipeline/pipeline_models/source.py
@docs/plans/2026-03-16-prompt-engineering-spec.md
@.claude/skills/extract-deal/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a whole-block chunk planner with exact 2-block overlap windows</name>
  <files>skill_pipeline/prompts/__init__.py, skill_pipeline/prompts/chunks.py</files>
  <read_first>skill_pipeline/pipeline_models/source.py, skill_pipeline/pipeline_models/prompt.py, skill_pipeline/compose_prompts.py, .planning/phases/02-prompt-architecture/02-CONTEXT.md, .planning/phases/02-prompt-architecture/02-RESEARCH.md</read_first>
  <action>Create `skill_pipeline/prompts/chunks.py` with a deterministic token estimator `estimate_block_tokens(block)` and a chunk planner `build_chunk_windows(blocks, chunk_budget, overlap_blocks=2)`. Use the exact overlap size `2`. Estimate tokens with a deterministic text-based heuristic such as `ceil(len(block.clean_text.split()) * 1.35)` (calibrated for typical English prose; SEC filing text with legal terms and dollar amounts may diverge — validate against actual `stec` packet sizes in Plan 03) and store the result on each `PromptChunkWindow`. If the full block list fits the budget, emit one `single_pass` window covering all block IDs. Otherwise emit ordered `chunked` windows whose `target_block_ids` contain only whole blocks and whose `overlap_block_ids` contain the 2 adjacent context blocks outside the target window. Do not split a block or synthesize overlap text independently of block IDs.</action>
  <verify>python -m pytest -q tests/test_skill_compose_prompts.py -k chunk</verify>
  <acceptance_criteria>
    - `skill_pipeline/prompts/chunks.py` contains `def estimate_block_tokens(`
    - `skill_pipeline/prompts/chunks.py` contains `def build_chunk_windows(`
    - `skill_pipeline/prompts/chunks.py` contains `overlap_blocks=2`
    - `tests/test_skill_compose_prompts.py` contains chunk-window assertions on `target_block_ids` and `overlap_block_ids`
  </acceptance_criteria>
  <done>The stage can deterministically decide when one packet is enough and when multiple chunked packets are required.</done>
</task>

<task type="auto">
  <name>Task 2: Render chronology-first packets, active evidence checklists, and stable prompt assets</name>
  <files>skill_pipeline/prompts/checklist.py, skill_pipeline/prompts/render.py, skill_pipeline/prompt_assets/actors_prefix.md, skill_pipeline/prompt_assets/events_prefix.md, skill_pipeline/prompt_assets/event_examples.md</files>
  <read_first>docs/plans/2026-03-16-prompt-engineering-spec.md, .claude/skills/extract-deal/SKILL.md, skill_pipeline/pipeline_models/source.py, skill_pipeline/pipeline_models/prompt.py, .planning/phases/02-prompt-architecture/02-CONTEXT.md</read_first>
  <action>Create `skill_pipeline/prompts/checklist.py` to convert `EvidenceItem` records into a concise active checklist grouped by `evidence_type`, preserving `evidence_id`, line range, and short imperative checklist bullets. Create `skill_pipeline/prompts/render.py` with `render_actor_packet(...)` and `render_event_packet(...)` that write packet sections in this exact body order: `<deal_context>`, `<chronology_blocks>`, optional `<overlap_context>`, `<evidence_checklist>`, optional `<actor_roster>`, `<task_instructions>`. The event renderer must include actor roster only for event packets. Add file-backed stable assets `skill_pipeline/prompt_assets/actors_prefix.md`, `skill_pipeline/prompt_assets/events_prefix.md`, and `skill_pipeline/prompt_assets/event_examples.md`. Keep all assets provider-neutral and avoid chat-role wrappers or API-specific syntax.</action>
  <verify>python -m pytest -q tests/test_skill_compose_prompts.py -k "render or overlap or checklist"</verify>
  <acceptance_criteria>
    - `skill_pipeline/prompts/render.py` contains `def render_actor_packet(`
    - `skill_pipeline/prompts/render.py` contains `def render_event_packet(`
    - rendered packets contain the exact tags `<chronology_blocks>`, `<evidence_checklist>`, and `<task_instructions>`
    - event packet tests assert `<actor_roster>` is present only for event packets and `<overlap_context>` is present only for chunked packets
  </acceptance_criteria>
  <done>The prompt engine has reusable stable assets and renders packets in the ordering required by the roadmap.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the renderer into compose-prompts and add deterministic packet regression tests</name>
  <files>skill_pipeline/compose_prompts.py, tests/test_skill_compose_prompts.py</files>
  <read_first>skill_pipeline/compose_prompts.py, skill_pipeline/prompts/chunks.py, skill_pipeline/prompts/checklist.py, skill_pipeline/prompts/render.py, .planning/phases/02-prompt-architecture/02-VALIDATION.md</read_first>
  <action>Update `run_compose_prompts(...)` so it loads chronology blocks and evidence items, builds chunk windows for the requested packet family, renders `prefix.md`, `body.md`, and `rendered.md` under `data/skill/<slug>/prompt/packets/<packet_id>/`, and writes a populated `manifest.json`. Support `--mode actors`, `--mode events`, and `--mode all`. Actor roster dependency: `--mode events` requires an existing `actors_raw.json` as the locked actor roster source; `--mode actors` must not. `--mode all` generates actor packets only — event packets require a separate `--mode events` call after actor extraction completes and `actors_raw.json` exists. If `--mode events` is called without `actors_raw.json`, fail fast with a clear error directing the user to run actor extraction first. Add `tests/test_skill_compose_prompts.py` fixture coverage that asserts (a) chronology blocks appear before task instructions, (b) chunked windows produce `<overlap_context>`, (c) checklist bullets include evidence IDs, (d) rendered packet files are written to the manifest-declared paths, and (e) `--mode events` fails when `actors_raw.json` is absent.</action>
  <verify>python -m pytest -q tests/test_skill_compose_prompts.py</verify>
  <acceptance_criteria>
    - `skill_pipeline/compose_prompts.py` contains `--mode` handling for `actors`, `events`, and `all`
    - `skill_pipeline/compose_prompts.py` writes `manifest.json`, `prefix.md`, `body.md`, and `rendered.md`
    - `tests/test_skill_compose_prompts.py` asserts chronology text precedes `<task_instructions>`
    - `tests/test_skill_compose_prompts.py` asserts evidence checklist entries include `evidence_id`
  </acceptance_criteria>
  <done>The stage produces real packet artifacts and deterministic render tests keep the contract stable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_skill_compose_prompts.py -k chunk` passes
- [ ] `python -m pytest -q tests/test_skill_compose_prompts.py -k "render or overlap or checklist"` passes
- [ ] `python -m pytest -q tests/test_skill_compose_prompts.py` passes
</verification>

<success_criteria>

- All tasks completed
- Packet rendering is chronology-first and provider-neutral
- Chunking is whole-block only with explicit 2-block overlap context
- Evidence checklist formatting is deterministic and test-covered
  </success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-architecture/02-02-SUMMARY.md`
</output>

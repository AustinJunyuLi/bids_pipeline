---
phase: 14-derivation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/models_v2.py
  - skill_pipeline/derive.py
  - tests/test_skill_observation_models.py
  - tests/test_skill_derive.py
autonomous: true
requirements: [DERIVE-01, DERIVE-03, DERIVE-06]

must_haves:
  truths:
    - "`models_v2.py` is aligned for deterministic derive rules before the runtime hardens the wrong contract"
    - "`derive.py` refuses to run unless the Phase 13 v2 validation artifacts pass"
    - "round rules produce `ProcessPhaseRecord` and cash rules produce `CashRegimeRecord` with explicit provenance"
    - "genuinely ambiguous derivation cases surface through `JudgmentRecord` instead of silent defaults while export-shaped row formatting stays out of the core derive artifact"
  artifacts:
    - path: "skill_pipeline/models_v2.py"
      provides: "graph-linked derived record contract and rule-relevant literal fields"
    - path: "skill_pipeline/derive.py"
      provides: "v2 derivation runtime and rule registry"
    - path: "tests/test_skill_observation_models.py"
      provides: "schema regression coverage for Phase 14 alignment fields"
    - path: "tests/test_skill_derive.py"
      provides: "phase, cash, and judgment regression coverage"
  key_links:
    - from: "skill_pipeline/models_v2.py"
      to: "tests/test_skill_observation_models.py"
      via: "Phase 14 schema-alignment fixtures"
      pattern: "derive-facing schema changes are pinned before runtime logic consumes them"
    - from: "skill_pipeline/derive.py"
      to: "tests/test_skill_derive.py"
      via: "synthetic canonical-v2 fixtures and passing validation artifacts"
      pattern: "round, cash, and judgment rules are pinned by direct fixture assertions"
---

<objective>
Align the derive-facing schema and build the first derivation families.

Purpose: Phase 14 should not harden an export-shaped row contract or a
summary-parsing fallback if a small additive schema fix would make the rules
deterministic. Phases, cash regimes, and explicit ambiguity handling then
define the structural backbone that later transition and export work depend on.

Output: `models_v2.py` alignment plus `derive.py` with validation gating, round
derivation, cash regime derivation, and judgment generation.
</objective>

<context>
@.planning/phases/14-derivation-engine/14-CONTEXT.md
@.planning/phases/14-derivation-engine/14-RESEARCH.md
@skill_pipeline/enrich_core.py
@skill_pipeline/models_v2.py
@skill_pipeline/check_v2.py
@skill_pipeline/coverage_v2.py
@skill_pipeline/gates_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the derive-facing schema before adding rules</name>
  <files>
    skill_pipeline/models_v2.py,
    skill_pipeline/derive.py
  </files>
  <action>
Update `models_v2.py` and the derive runtime together so Phase 14 has:
- graph-linked derived records centered on provenance and observation/cohort
  references rather than export-shaped bidder/date/value columns
- additive literal fields needed for deterministic rules when those fields are
  missing today

Then add `derive.py` that:
- requires passing `check_v2`, `coverage_v2`, and `gates_v2`
- derives informal/formal `ProcessPhaseRecord` from solicitation observations
- derives phase-level `CashRegimeRecord` from proposal terms and merger-agreement
  summaries
- emits `JudgmentRecord` when phase or advisory/initiation state is ambiguous
  </action>
  <acceptance_criteria>
    - no derivation artifact is written when required validation stages fail
    - every derived record carries `DerivationBasis`
    - derive-facing records are graph-linked rather than benchmark/export-shaped
    - the all-cash inference remains phase-local and does not short-circuit like v1
  </acceptance_criteria>
  <done>Phase 14 starts from the right schema boundary and a safe gated derive core</done>
</task>

<task type="auto">
  <name>Task 2: Add schema and rule regression tests</name>
  <files>
    tests/test_skill_observation_models.py,
    tests/test_skill_derive.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_observation_models.py tests/test_skill_derive.py -k 'phase or cash or judgment or validation or row_count or subject_ref'</automated>
  </verify>
  <acceptance_criteria>
    - tests cover the aligned graph-linked derived row contract
    - tests cover derive gating
    - tests cover informal/formal phase derivation
    - tests cover explicit and inferred all-cash regimes
    - tests cover at least one ambiguity judgment
  </acceptance_criteria>
  <done>the derive core is pinned by schema and rule regression tests</done>
</task>

</tasks>

---
phase: 14-derivation-engine
plan: 03
type: execute
wave: 1
depends_on: [14-01, 14-02]
files_modified:
  - skill_pipeline/derive.py
  - skill_pipeline/cli.py
  - tests/test_skill_derive.py
  - tests/test_skill_pipeline.py
  - CLAUDE.md
autonomous: true
requirements: [DERIVE-01, DERIVE-02, DERIVE-03, DERIVE-04, DERIVE-05, DERIVE-06]

must_haves:
  truths:
    - "`derive` is a live additive CLI command writing `derive/derivations.json` and `derive/derive_log.json`"
    - "Phase 14 verification proves the derivation engine runs end-to-end on synthetic validated fixtures"
    - "repo memory reflects that derivation artifacts are now live writers while benchmark-expanded anonymous slot emission remains an export concern"
  artifacts:
    - path: "skill_pipeline/cli.py"
      provides: "CLI registration for derive"
    - path: "tests/test_skill_pipeline.py"
      provides: "parser regression for derive"
    - path: "CLAUDE.md"
      provides: "updated repo memory for live derive outputs"
  key_links:
    - from: "skill_pipeline/cli.py"
      to: "tests/test_skill_pipeline.py"
      via: "derive CLI parser coverage"
      pattern: "the additive derive command is live without affecting v1 commands"
---

<objective>
Finish Phase 14 as a runnable derivation stage.

Purpose: The milestone needs a live derivation command and documented artifact
contract before DuckDB loading starts, but the repo memory must preserve the
Phase 14 versus Phase 15 boundary.

Output: derive CLI wiring, derive-log writing, parser coverage, and memory
updates that preserve the derivation/export split.
</objective>

<context>
@.planning/phases/14-derivation-engine/14-CONTEXT.md
@.planning/phases/14-derivation-engine/14-RESEARCH.md
@skill_pipeline/derive.py
@skill_pipeline/cli.py
@tests/test_skill_pipeline.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose derive as a live runtime stage</name>
  <files>
    skill_pipeline/derive.py,
    skill_pipeline/cli.py
  </files>
  <action>
Finish `derive.py` artifact writing and add the `derive` CLI command.
The runtime must write `derive/derivations.json` and `derive/derive_log.json`
for validated canonical-v2 fixtures.
  </action>
  <acceptance_criteria>
    - derive writes both artifact files
    - CLI dispatch reaches the derive runtime
    - derive writes graph-linked rows without benchmark-expanded anonymous slot output
    - v1 CLI behavior remains unchanged
  </acceptance_criteria>
  <done>derive is a live additive runtime stage</done>
</task>

<task type="auto">
  <name>Task 2: Add parser coverage and update memory</name>
  <files>
    tests/test_skill_derive.py,
    tests/test_skill_pipeline.py,
    CLAUDE.md
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_derive.py tests/test_skill_pipeline.py -k 'derive'</automated>
  </verify>
  <acceptance_criteria>
    - parser tests cover derive
    - the synthetic derive fixture runs end-to-end
    - CLAUDE.md marks the derive outputs as live canonical-v2 artifact writers
      while keeping benchmark-expanded anonymous expansion in Phase 15/export_v2
  </acceptance_criteria>
  <done>Phase 14 runtime is tested and documented</done>
</task>

</tasks>

---
phase: 14-derivation-engine
plan: 02
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - skill_pipeline/models_v2.py
  - skill_pipeline/derive.py
  - tests/test_skill_derive.py
autonomous: true
requirements: [DERIVE-02, DERIVE-04, DERIVE-05]

must_haves:
  truths:
    - "exit rules produce `LifecycleTransitionRecord` for literal, cannot-improve, not-invited, and lost-to-winner cases"
    - "analyst-row compilation turns literal observations and derived records into provenance-bearing, graph-linked `AnalystRowRecord` rows"
    - "cohort-backed rows preserve `subject_ref` and `row_count` for later export expansion instead of inventing slot rows or named bidder identities"
  artifacts:
    - path: "skill_pipeline/models_v2.py"
      provides: "aligned Phase 14 row and transition contracts"
    - path: "skill_pipeline/derive.py"
      provides: "exit rules and analyst-row compiler"
    - path: "tests/test_skill_derive.py"
      provides: "transition and analyst-row regression coverage"
  key_links:
    - from: "skill_pipeline/derive.py"
      to: "tests/test_skill_derive.py"
      via: "synthetic status/outcome/cohort fixtures"
      pattern: "each named exit rule and graph-linked row behavior is covered directly"
---

<objective>
Finish the derivation semantics that directly power later export work without
collapsing back into spreadsheet-shaped rows.

Purpose: The derived transitions and analyst rows are the bridge from the
observation graph to later DuckDB and legacy-adapter work, so this plan must
preserve the graph boundary instead of rebuilding v1 semantics on a new schema.

Output: lifecycle transitions and graph-linked analyst-row compilation in
`derive.py`.
</objective>

<context>
@.planning/phases/14-derivation-engine/14-CONTEXT.md
@.planning/phases/14-derivation-engine/14-RESEARCH.md
@skill_pipeline/derive.py
@skill_pipeline/db_export.py
@skill_pipeline/models_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exit rules and graph-linked analyst-row compilation</name>
  <files>
    skill_pipeline/models_v2.py,
    skill_pipeline/derive.py
  </files>
  <action>
Extend `derive.py` (and `models_v2.py` if the row or transition contract still
needs additive alignment) with:
- EXIT-01 through EXIT-04 lifecycle transition rules
- AGREEMENT-01 row handling for superseding NDA-family agreements
- graph-linked analyst-row compilation for literal observations, derived
  phases, transitions, and cohort-backed rows with `row_count`
- bidder-type formatting only where it preserves later export compatibility
  without turning the core row contract into the export surface
  </action>
  <acceptance_criteria>
    - round-derived rows emit announcement/deadline row pairs
    - cohort-backed rows do not expand into anonymous slot rows inside the core
      derive artifact
    - transitions do not invent named bidders when only cohorts are known
  </acceptance_criteria>
  <done>derive now produces the analyst row surface needed by export work</done>
</task>

<task type="auto">
  <name>Task 2: Add transition and row regression tests</name>
  <files>
    tests/test_skill_derive.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_derive.py -k 'exit or analyst or agreement or row_count or subject_ref'</automated>
  </verify>
  <acceptance_criteria>
    - tests cover literal and inferred exits
    - tests cover derived round rows
    - tests cover literal and derived analyst rows plus cohort-backed row counts
  </acceptance_criteria>
  <done>derived transitions and graph-linked row compilation are regression-tested</done>
</task>

</tasks>

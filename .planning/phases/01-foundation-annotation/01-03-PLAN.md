---
phase: 01-foundation-annotation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/validate_annotated_blocks.py
  - data/deals/imprivata/source/chronology_blocks.jsonl
  - data/deals/mac-gray/source/chronology_blocks.jsonl
  - data/deals/medivation/source/chronology_blocks.jsonl
  - data/deals/penford/source/chronology_blocks.jsonl
  - data/deals/petsmart-inc/source/chronology_blocks.jsonl
  - data/deals/providence-worcester/source/chronology_blocks.jsonl
  - data/deals/saks/source/chronology_blocks.jsonl
  - data/deals/stec/source/chronology_blocks.jsonl
  - data/deals/zep/source/chronology_blocks.jsonl
autonomous: true
requirements:
  - INFRA-04
  - INFRA-06
user_setup: []
must_haves:
  truths:
    - "all 9 active deals have chronology_blocks.jsonl files that parse under the new annotated ChronologyBlock schema"
    - "stec provides a human-auditable annotated sample with real dates, entities, non-zero density, and plausible phase hints"
    - "the full pytest suite passes after source-artifact regeneration"
  artifacts:
    - path: "scripts/validate_annotated_blocks.py"
      provides: "repeatable validation of annotated chronology block artifacts"
      exports: ["main"]
    - path: "data/deals/stec/source/chronology_blocks.jsonl"
      provides: "annotated baseline deal chronology blocks"
      contains: "\"temporal_phase\""
    - path: "data/deals/imprivata/source/chronology_blocks.jsonl"
      provides: "annotated regenerated deal chronology blocks"
      contains: "\"date_mentions\""
  key_links:
    - from: "scripts/validate_annotated_blocks.py"
      to: "skill_pipeline/pipeline_models/source.py"
      via: "ChronologyBlock.model_validate_json"
      pattern: "ChronologyBlock"
    - from: "skill_pipeline/preprocess/source.py"
      to: "data/deals/stec/source/chronology_blocks.jsonl"
      via: "skill-pipeline preprocess-source regeneration"
      pattern: "chronology_blocks.jsonl"
---

<objective>
Regenerate the full deal corpus under the annotated source contract and prove
the phase exit criteria hold on real artifacts.

Purpose: Phase 1 is incomplete until the code changes have been applied to all
9 active deals and validated against both tests and a concrete `stec` sample.
Output: a reusable validation script plus regenerated annotated
`chronology_blocks.jsonl` files for every active deal.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-annotation/01-CONTEXT.md
@.planning/phases/01-foundation-annotation/01-RESEARCH.md
@.planning/phases/01-foundation-annotation/01-VALIDATION.md
@skill_pipeline/preprocess/source.py
@skill_pipeline/pipeline_models/source.py
@data/seeds.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repeatable validator for annotated chronology block artifacts</name>
  <files>scripts/validate_annotated_blocks.py</files>
  <read_first>skill_pipeline/pipeline_models/source.py, skill_pipeline/preprocess/source.py, data/seeds.csv, .planning/phases/01-foundation-annotation/01-VALIDATION.md</read_first>
  <action>Create `scripts/validate_annotated_blocks.py` with an explicit default deal list of `imprivata`, `mac-gray`, `medivation`, `penford`, `petsmart-inc`, `providence-worcester`, `saks`, `stec`, and `zep`. The script must load each `chronology_blocks.jsonl` line through `ChronologyBlock.model_validate_json`, fail if any block is missing the required metadata keys, and support `--spot-check stec` to assert that at least one `stec` block has non-empty `date_mentions`, non-empty `entity_mentions`, `evidence_density > 0`, and `temporal_phase` in the allowed enum.</action>
  <verify>python scripts/validate_annotated_blocks.py --deals stec --spot-check stec</verify>
  <acceptance_criteria>
    - `scripts/validate_annotated_blocks.py` exists
    - `scripts/validate_annotated_blocks.py` contains the explicit default deal list with all 9 slugs
    - `scripts/validate_annotated_blocks.py` contains `ChronologyBlock.model_validate_json`
    - `scripts/validate_annotated_blocks.py` contains `--spot-check`
  </acceptance_criteria>
  <done>The repo has a reusable artifact validator that can catch missing annotation fields before downstream phases consume regenerated source data.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run preprocess-source for every active deal and stop on the first broken corpus input</name>
  <files>data/deals/imprivata/source/chronology_blocks.jsonl, data/deals/mac-gray/source/chronology_blocks.jsonl, data/deals/medivation/source/chronology_blocks.jsonl, data/deals/penford/source/chronology_blocks.jsonl, data/deals/petsmart-inc/source/chronology_blocks.jsonl, data/deals/providence-worcester/source/chronology_blocks.jsonl, data/deals/saks/source/chronology_blocks.jsonl, data/deals/stec/source/chronology_blocks.jsonl, data/deals/zep/source/chronology_blocks.jsonl</files>
  <read_first>data/seeds.csv, skill_pipeline/preprocess/source.py, raw/stec/document_registry.json, data/deals/stec/source/chronology_blocks.jsonl</read_first>
  <action>Run `skill-pipeline preprocess-source --deal <slug>` for each active deal in this exact order: `imprivata`, `mac-gray`, `medivation`, `penford`, `petsmart-inc`, `providence-worcester`, `saks`, `stec`, `zep`. Let the command rewrite the existing source artifacts in place. Do not touch `raw/<slug>/filings/*.txt`. If any deal fails because discovery, registry, chronology selection, or annotation assumptions break, stop immediately, capture the failing slug and exception text in the summary, and do not silently skip or continue.</action>
  <verify>python scripts/validate_annotated_blocks.py --deals imprivata mac-gray medivation penford petsmart-inc providence-worcester saks stec zep</verify>
  <acceptance_criteria>
    - each listed `data/deals/<slug>/source/chronology_blocks.jsonl` file contains `"date_mentions"`
    - each listed `data/deals/<slug>/source/chronology_blocks.jsonl` file contains `"entity_mentions"`
    - each listed `data/deals/<slug>/source/chronology_blocks.jsonl` file contains `"evidence_density"`
    - each listed `data/deals/<slug>/source/chronology_blocks.jsonl` file contains `"temporal_phase"`
  </acceptance_criteria>
  <done>All 9 active deals have regenerated chronology block artifacts under the new contract, or the run stops at the first failing input with a precise report.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the regenerated corpus with full tests and the required stec spot-check</name>
  <files>scripts/validate_annotated_blocks.py, data/deals/stec/source/chronology_blocks.jsonl</files>
  <read_first>scripts/validate_annotated_blocks.py, data/deals/stec/source/chronology_blocks.jsonl, .planning/ROADMAP.md, .planning/phases/01-foundation-annotation/01-VALIDATION.md</read_first>
  <action>Run `python scripts/validate_annotated_blocks.py --deals imprivata mac-gray medivation penford petsmart-inc providence-worcester saks stec zep --spot-check stec`, then run `python -m pytest -q`. After the automated checks pass, manually inspect one early, one middle, and one late `stec` block in `data/deals/stec/source/chronology_blocks.jsonl` to confirm that the phase hint follows the narrative and that date/entity extraction is not trivially empty. Record the exact slugs processed, validator command, pytest command, and `stec` spot-check notes in `01-03-SUMMARY.md`.</action>
  <verify>python -m pytest -q</verify>
  <acceptance_criteria>
    - `python scripts/validate_annotated_blocks.py --deals imprivata mac-gray medivation penford petsmart-inc providence-worcester saks stec zep --spot-check stec` exits 0
    - `python -m pytest -q` exits 0
    - `data/deals/stec/source/chronology_blocks.jsonl` contains non-empty `date_mentions`, non-empty `entity_mentions`, and `temporal_phase` values across the inspected sample
  </acceptance_criteria>
  <done>The phase exit criteria are satisfied on real regenerated artifacts, not only unit fixtures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python scripts/validate_annotated_blocks.py --deals imprivata mac-gray medivation penford petsmart-inc providence-worcester saks stec zep --spot-check stec` passes
- [ ] `python -m pytest -q` passes
- [ ] `stec` spot-check notes are captured in the summary
</verification>

<success_criteria>

- All tasks completed
- All 9 active deals have annotated chronology block artifacts
- The repo passes its full regression suite after regeneration
- `stec` is spot-checked as the baseline validation deal
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-annotation/01-03-SUMMARY.md`
</output>

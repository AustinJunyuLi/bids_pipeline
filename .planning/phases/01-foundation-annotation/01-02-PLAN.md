---
phase: 01-foundation-annotation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .planning/codebase/STACK.md
  - docs/design.md
  - docs/plans/2026-03-16-pipeline-design-v3.md
  - docs/plans/2026-03-16-prompt-engineering-spec.md
  - tests/test_runtime_contract_docs.py
  - tests/test_benchmark_separation_policy.py
autonomous: true
requirements:
  - INFRA-06
user_setup: []
must_haves:
  truths:
    - "runtime dependency manifests describe only the live deterministic Python path and cap edgartools below v6"
    - "repo-truth docs clearly distinguish live skill_pipeline runtime facts from historical provider-wrapper design notes"
    - "tests fail if provider-specific runtime dependencies or stale historical-doc claims re-enter the live contract"
  artifacts:
    - path: "pyproject.toml"
      provides: "live deterministic runtime dependency manifest"
      contains: "edgartools>=5.23,<6.0"
    - path: "requirements.txt"
      provides: "runtime requirements mirror for local installs"
      contains: "edgartools>=5.23,<6.0"
    - path: "tests/test_runtime_contract_docs.py"
      provides: "regression coverage for runtime manifest and docs boundaries"
      min_lines: 30
  key_links:
    - from: "tests/test_runtime_contract_docs.py"
      to: "pyproject.toml"
      via: "manifest assertions"
      pattern: "pyproject.toml"
    - from: "docs/design.md"
      to: "docs/plans/2026-03-16-pipeline-design-v3.md"
      via: "historical background disclaimer"
      pattern: "historical"
---

<objective>
Harden the live deterministic runtime contract in manifests and docs without
expanding Phase 1 into provider-runtime work.

Purpose: downstream agents and humans must not confuse historical provider
wrapper notes with the live `skill_pipeline` + local-agent split.
Output: tightened dependency manifests, explicit historical-doc disclaimers,
and regression tests for the runtime-contract boundary.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-annotation/01-CONTEXT.md
@.planning/phases/01-foundation-annotation/01-RESEARCH.md
@CLAUDE.md
@pyproject.toml
@requirements.txt
@.planning/codebase/STACK.md
@docs/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten live runtime dependency manifests to the deterministic path</name>
  <files>pyproject.toml, requirements.txt</files>
  <read_first>pyproject.toml, requirements.txt, CLAUDE.md, .planning/codebase/CONCERNS.md, skill_pipeline/raw/stage.py</read_first>
  <action>Audit live imports with `rg -n "^(from|import) anthropic|^(from|import) openai" skill_pipeline tests`. If that command returns no matches, remove `anthropic>=0.49` from both `pyproject.toml` and `requirements.txt`. In all cases, change `edgartools>=5.23` to `edgartools>=5.23,<6.0`. Do not add any OpenAI or Anthropic dependency to the Python runtime manifests. Leave `openpyxl>=3.1` in place unless a live workflow audit proves it is completely unused.</action>
  <verify>python -m pytest -q tests/test_runtime_contract_docs.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `edgartools>=5.23,<6.0`
    - `requirements.txt` contains `edgartools>=5.23,<6.0`
    - neither `pyproject.toml` nor `requirements.txt` contains `anthropic>=`
    - neither `pyproject.toml` nor `requirements.txt` contains `openai>=`
  </acceptance_criteria>
  <done>Runtime manifests reflect only the deterministic Python path and cap the fragile EdgarTools dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Mark stale provider-wrapper design docs as historical without rewriting them wholesale</name>
  <files>.planning/codebase/STACK.md, docs/design.md, docs/plans/2026-03-16-pipeline-design-v3.md, docs/plans/2026-03-16-prompt-engineering-spec.md</files>
  <read_first>.planning/codebase/STACK.md, docs/design.md, docs/plans/2026-03-16-pipeline-design-v3.md, docs/plans/2026-03-16-prompt-engineering-spec.md, CLAUDE.md</read_first>
  <action>Update `.planning/codebase/STACK.md` so the runtime-contract section states that only `PIPELINE_SEC_IDENTITY`, `SEC_IDENTITY`, and `EDGAR_IDENTITY` are repo-verified Python runtime variables. Keep `docs/design.md` as the design index but make sure it explicitly says the 2026-03-16 plan docs are historical background, not live implementation authority. Add a short top-of-file warning block to both `docs/plans/2026-03-16-pipeline-design-v3.md` and `docs/plans/2026-03-16-prompt-engineering-spec.md` stating that `pipeline/` paths, provider backends, and model-pinned guidance are historical and superseded by `skill_pipeline` plus `.claude/skills/`.</action>
  <verify>python -m pytest -q tests/test_runtime_contract_docs.py tests/test_benchmark_separation_policy.py</verify>
  <acceptance_criteria>
    - `.planning/codebase/STACK.md` contains the exact env var names `PIPELINE_SEC_IDENTITY`, `SEC_IDENTITY`, and `EDGAR_IDENTITY`
    - `docs/design.md` contains `historical background` and `not the authoritative description` or equivalent exact wording
    - `docs/plans/2026-03-16-pipeline-design-v3.md` contains `historical` and `skill_pipeline`
    - `docs/plans/2026-03-16-prompt-engineering-spec.md` contains `historical` and `.claude/skills/`
  </acceptance_criteria>
  <done>Historical docs can no longer be mistaken for live implementation truth.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for manifest and docs boundary rules</name>
  <files>tests/test_runtime_contract_docs.py, tests/test_benchmark_separation_policy.py</files>
  <read_first>tests/test_benchmark_separation_policy.py, pyproject.toml, requirements.txt, CLAUDE.md, docs/design.md, docs/plans/2026-03-16-pipeline-design-v3.md, docs/plans/2026-03-16-prompt-engineering-spec.md</read_first>
  <action>Create `tests/test_runtime_contract_docs.py` with assertions that `pyproject.toml` and `requirements.txt` do not declare `anthropic` or `openai`, both manifests cap `edgartools` below 6.0, and the two historical plan docs contain the disclaimer text added in Task 2. Extend `tests/test_benchmark_separation_policy.py` only if needed to keep benchmark-boundary expectations aligned with the new historical-doc wording; do not weaken its current enforcement of benchmark separation.</action>
  <verify>python -m pytest -q tests/test_runtime_contract_docs.py tests/test_benchmark_separation_policy.py tests/test_skill_mirror_sync.py</verify>
  <acceptance_criteria>
    - `tests/test_runtime_contract_docs.py` exists
    - `tests/test_runtime_contract_docs.py` contains assertions on `pyproject.toml` and `requirements.txt`
    - `tests/test_runtime_contract_docs.py` contains assertions on both 2026-03-16 plan docs
  </acceptance_criteria>
  <done>Runtime contract and historical-doc boundaries are enforced by tests, not only prose.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_runtime_contract_docs.py tests/test_benchmark_separation_policy.py` passes
- [ ] `pyproject.toml` and `requirements.txt` both cap `edgartools` below 6.0
- [ ] historical-doc warnings are present in both 2026-03-16 plan docs
</verification>

<success_criteria>

- All tasks completed
- Runtime manifests no longer imply a Python-side LLM runtime
- Historical docs are clearly labeled as non-authoritative
- Regression tests enforce the hardened contract
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-annotation/01-02-SUMMARY.md`
</output>

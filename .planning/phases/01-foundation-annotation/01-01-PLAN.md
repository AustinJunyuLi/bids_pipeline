---
phase: 01-foundation-annotation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/pipeline_models/source.py
  - skill_pipeline/preprocess/source.py
  - skill_pipeline/source/annotate.py
  - tests/test_skill_source_annotations.py
  - tests/test_skill_preprocess_source.py
  - tests/test_skill_canonicalize.py
  - tests/test_skill_verify.py
  - tests/test_skill_coverage.py
  - tests/test_skill_enrich_core.py
  - tests/test_skill_pipeline.py
  - tests/test_skill_provenance.py
autonomous: true
requirements:
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "preprocess-source writes chronology blocks with required date_mentions, entity_mentions, evidence_density, and temporal_phase metadata"
    - "ChronologyBlock validation fails when serialized blocks omit the new metadata fields"
    - "all deterministic stage tests that deserialize chronology blocks still pass under the annotated schema"
  artifacts:
    - path: "skill_pipeline/source/annotate.py"
      provides: "deterministic chronology block annotation helpers"
      exports: ["annotate_chronology_blocks"]
    - path: "skill_pipeline/pipeline_models/source.py"
      provides: "required annotated ChronologyBlock schema"
      contains: "date_mentions"
    - path: "skill_pipeline/preprocess/source.py"
      provides: "annotation integrated into preprocess_source_deal"
      contains: "annotate_chronology_blocks"
    - path: "tests/test_skill_source_annotations.py"
      provides: "unit coverage for block annotation helpers"
      min_lines: 40
  key_links:
    - from: "skill_pipeline/preprocess/source.py"
      to: "skill_pipeline/source/annotate.py"
      via: "annotate_chronology_blocks import and call"
      pattern: "annotate_chronology_blocks"
    - from: "tests/test_skill_preprocess_source.py"
      to: "skill_pipeline/preprocess/source.py"
      via: "fixture-driven preprocess regression"
      pattern: "preprocess_source_deal"
---

<objective>
Implement the annotated `ChronologyBlock` contract and wire it into the live
`preprocess-source` stage.

Purpose: Phase 2 prompt work depends on deterministic block metadata, and the
repo must fail fast on stale unannotated source artifacts.
Output: required metadata fields on `ChronologyBlock`, deterministic annotation
helpers, preprocess integration, and refreshed test fixtures.
</objective>

<execution_context>
@/mnt/c/Users/ALi/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/ALi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-annotation/01-CONTEXT.md
@.planning/phases/01-foundation-annotation/01-RESEARCH.md
@skill_pipeline/pipeline_models/source.py
@skill_pipeline/preprocess/source.py
@skill_pipeline/source/blocks.py
@skill_pipeline/source/evidence.py
@skill_pipeline/normalize/dates.py
@skill_pipeline/seeds.py
@tests/test_skill_preprocess_source.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the required annotated block schema and helper surface</name>
  <files>skill_pipeline/pipeline_models/source.py, skill_pipeline/source/annotate.py</files>
  <read_first>skill_pipeline/pipeline_models/source.py, skill_pipeline/normalize/dates.py, skill_pipeline/source/evidence.py, skill_pipeline/seeds.py, data/seeds.csv, .planning/phases/01-foundation-annotation/01-CONTEXT.md</read_first>
  <action>Add `BlockDateMention` and `BlockEntityMention` models in `skill_pipeline/pipeline_models/source.py`, then make `ChronologyBlock` require `date_mentions`, `entity_mentions`, `evidence_density`, and `temporal_phase`. Use `temporal_phase: Literal["initiation", "bidding", "outcome", "other"]`. Create `skill_pipeline/source/annotate.py` with `annotate_chronology_blocks(...)` plus helper functions that (a) find date fragments with `DATE_FRAGMENT_RE`, (b) normalize them with `parse_resolved_date`, (c) match target/acquirer seed names plus filing aliases `Party [A-Z]`, `the Company`, `the Board`, `Special Committee`, and `Transaction Committee`, (d) count overlapping evidence items by line range, and (e) map overlapping evidence to `initiation`, `bidding`, or `outcome` before using ordinal fallback to `other`. Do not make the new fields optional or give them defaults.</action>
  <verify>python -m pytest -q tests/test_skill_source_annotations.py</verify>
  <acceptance_criteria>
    - `skill_pipeline/pipeline_models/source.py` contains `date_mentions`, `entity_mentions`, `evidence_density`, and `temporal_phase`
    - `skill_pipeline/source/annotate.py` contains `def annotate_chronology_blocks(`
    - `skill_pipeline/source/annotate.py` contains the exact phase labels `initiation`, `bidding`, `outcome`, and `other`
  </acceptance_criteria>
  <done>Schema and helper module exist, and the unit test file for annotation helpers passes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate annotation into preprocess-source without changing the CLI contract</name>
  <files>skill_pipeline/preprocess/source.py</files>
  <read_first>skill_pipeline/preprocess/source.py, skill_pipeline/source/blocks.py, skill_pipeline/seeds.py, skill_pipeline/cli.py, .planning/phases/01-foundation-annotation/01-CONTEXT.md</read_first>
  <action>Inside `preprocess_source_deal`, load the deal seed row with `load_seed_entry(deal_slug, seeds_path=project_root / "data" / "seeds.csv")`, annotate the chronology blocks after `_dedupe_evidence_items(...)` and before `_atomic_write_jsonl(...)`, then serialize the annotated block payloads back to the existing `chronology_blocks.jsonl` path. Keep annotation inside `preprocess-source`; do not add a new subcommand, a new artifact file, or any fallback loader for stale blocks. Preserve the current invalidation behavior so a failed annotation pass still deletes partially written source artifacts.</action>
  <verify>python -m pytest -q tests/test_skill_preprocess_source.py</verify>
  <acceptance_criteria>
    - `skill_pipeline/preprocess/source.py` contains `load_seed_entry(`
    - `skill_pipeline/preprocess/source.py` contains `annotate_chronology_blocks(`
    - `skill_pipeline/preprocess/source.py` still writes `chronology_blocks.jsonl` and does not add a second annotated block artifact
  </acceptance_criteria>
  <done>The preprocess stage emits annotated blocks through the existing CLI entrypoint and still fails fast on source-stage exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused annotation tests and refresh every stale block fixture</name>
  <files>tests/test_skill_source_annotations.py, tests/test_skill_preprocess_source.py, tests/test_skill_canonicalize.py, tests/test_skill_verify.py, tests/test_skill_coverage.py, tests/test_skill_enrich_core.py, tests/test_skill_pipeline.py, tests/test_skill_provenance.py</files>
  <read_first>tests/test_skill_preprocess_source.py, tests/test_skill_canonicalize.py, tests/test_skill_verify.py, tests/test_skill_coverage.py, tests/test_skill_enrich_core.py, tests/test_skill_pipeline.py, tests/test_skill_provenance.py, .planning/phases/01-foundation-annotation/01-RESEARCH.md</read_first>
  <action>Create `tests/test_skill_source_annotations.py` with unit tests for date extraction, entity matching, evidence density counting, and temporal phase assignment. Extend `tests/test_skill_preprocess_source.py` to assert concrete annotated outputs on the existing fixture chronology: the block containing `On July 1, 2016, Party A signed a confidentiality agreement.` must emit a `July 1, 2016` date mention, a `Party A` entity mention, `evidence_density > 0`, and `temporal_phase == "bidding"`. Update every other test that writes `chronology_blocks.jsonl` so its block fixtures include the new required metadata keys rather than `{}` or the legacy bare block schema.</action>
  <verify>python -m pytest -q tests/test_skill_source_annotations.py tests/test_skill_preprocess_source.py tests/test_skill_canonicalize.py tests/test_skill_verify.py tests/test_skill_coverage.py tests/test_skill_enrich_core.py tests/test_skill_pipeline.py tests/test_skill_provenance.py</verify>
  <acceptance_criteria>
    - `tests/test_skill_source_annotations.py` exists and contains tests for dates, entities, density, and phase assignment
    - `tests/test_skill_preprocess_source.py` contains `temporal_phase == "bidding"` or an equivalent assertion on the July 1 fixture block
    - each of `tests/test_skill_canonicalize.py`, `tests/test_skill_verify.py`, `tests/test_skill_coverage.py`, `tests/test_skill_enrich_core.py`, `tests/test_skill_pipeline.py`, and `tests/test_skill_provenance.py` contains the new block metadata keys
  </acceptance_criteria>
  <done>All block-consuming tests serialize schema-valid annotated blocks and the targeted regression suite passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_skill_source_annotations.py tests/test_skill_preprocess_source.py` passes
- [ ] `python -m pytest -q tests/test_skill_canonicalize.py tests/test_skill_verify.py tests/test_skill_coverage.py tests/test_skill_enrich_core.py tests/test_skill_pipeline.py tests/test_skill_provenance.py` passes
- [ ] `skill_pipeline/preprocess/source.py` still exposes one `preprocess-source` path and no extra annotation CLI
</verification>

<success_criteria>

- All tasks completed
- Annotated block schema is required and serialized through `chronology_blocks.jsonl`
- The preprocess stage remains deterministic and fail-fast
- Downstream deterministic tests accept the new source artifact contract
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-annotation/01-01-SUMMARY.md`
</output>

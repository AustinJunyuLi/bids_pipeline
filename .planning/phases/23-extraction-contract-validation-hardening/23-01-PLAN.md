---
phase: 23-extraction-contract-validation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/compose_prompts.py
  - skill_pipeline/prompt_assets/observations_v2_prefix.md
  - skill_pipeline/prompt_assets/observations_v2_examples.md
  - skill_pipeline/gates_v2.py
  - .claude/skills/extract-deal-v2/SKILL.md
  - .claude/skills/verify-extraction-v2/SKILL.md
  - .codex/skills/extract-deal-v2/SKILL.md
  - .codex/skills/verify-extraction-v2/SKILL.md
  - .cursor/skills/extract-deal-v2/SKILL.md
  - .cursor/skills/verify-extraction-v2/SKILL.md
  - tests/test_skill_gates_v2.py
  - tests/test_skill_phase16_migration.py
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02]

must_haves:
  truths:
    - "the observation prompt explicitly instructs recipient refs, chronology-safe proposal links, bidder-scoped outcomes, agreement-family distinctions, and relative-date anchoring"
    - "the active extract/verify skill docs reinforce the stronger v2 contract"
    - "`gates-v2` surfaces missing named recipients, under-specified substantive outcomes, proxy-date leakage, and lossy agreement-kind classification"
    - "focused prompt/gate tests pin the new contract"
  artifacts:
    - path: "skill_pipeline/compose_prompts.py"
      provides: "stronger observation task instructions"
    - path: "skill_pipeline/prompt_assets/observations_v2_prefix.md"
      provides: "top-level contract reminders"
    - path: "skill_pipeline/prompt_assets/observations_v2_examples.md"
      provides: "field-specific schema reminders"
    - path: "skill_pipeline/gates_v2.py"
      provides: "new contract-hardening findings"
    - path: ".claude/skills/extract-deal-v2/SKILL.md"
      provides: "active extraction skill contract"
    - path: ".claude/skills/verify-extraction-v2/SKILL.md"
      provides: "active verification skill contract"
    - path: "tests/test_skill_gates_v2.py"
      provides: "gate hardening regression coverage"
    - path: "tests/test_skill_phase16_migration.py"
      provides: "v2 prompt rendering regression coverage"
  key_links:
    - from: "skill_pipeline/compose_prompts.py"
      to: "tests/test_skill_phase16_migration.py"
      via: "rendered prompt contract assertions"
      pattern: "the stronger prompt instructions stay visible in the live packet output"
    - from: "skill_pipeline/gates_v2.py"
      to: "tests/test_skill_gates_v2.py"
      via: "synthetic malformed-extraction fixtures"
      pattern: "contract-hardening findings are pinned before derive"
---

<objective>
Harden the extraction contract and validation layer.

Purpose: GPT Pro showed that several remaining errors are upstream contract
problems rather than pure derive logic. This plan makes those requirements
explicit in the prompt and skill docs, then surfaces the malformed cases in
`gates-v2` before they can drift into later stages.

Output: stronger prompt/skill guidance plus new gate findings and tests.
</objective>

<context>
@.planning/phases/23-extraction-contract-validation-hardening/23-CONTEXT.md
@skill_pipeline/compose_prompts.py
@skill_pipeline/gates_v2.py
@tests/test_skill_gates_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the live observation contract</name>
  <files>
    skill_pipeline/compose_prompts.py,
    skill_pipeline/prompt_assets/observations_v2_prefix.md,
    skill_pipeline/prompt_assets/observations_v2_examples.md,
    .claude/skills/extract-deal-v2/SKILL.md,
    .claude/skills/verify-extraction-v2/SKILL.md,
    .codex/skills/extract-deal-v2/SKILL.md,
    .codex/skills/verify-extraction-v2/SKILL.md,
    .cursor/skills/extract-deal-v2/SKILL.md,
    .cursor/skills/verify-extraction-v2/SKILL.md
  </files>
  <action>
Update the prompt assets and mirrored skill docs so they explicitly require:
- `recipient_refs` when invitees are named or a reusable cohort is given
- chronology-safe `requested_by_observation_id` links that never point forward
- distinct agreement-family extraction
- bidder or bidder-cohort refs on named executed/restarted outcomes
- relative-date anchoring instead of pretending proxy dates are exact
  </action>
  <acceptance_criteria>
    - the stronger instructions are visible in the rendered v2 prompt packet
    - extract and verify docs reinforce the same contract rules
  </acceptance_criteria>
  <done>the live extraction contract matches the stronger downstream runtime assumptions</done>
</task>

<task type="auto">
  <name>Task 2: Add gate findings and pin them with tests</name>
  <files>
    skill_pipeline/gates_v2.py,
    tests/test_skill_gates_v2.py,
    tests/test_skill_phase16_migration.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_gates_v2.py tests/test_skill_phase16_migration.py -k 'recipient or outcome or proxy or agreement or prompt'</automated>
  </verify>
  <action>
Extend `gates_v2.py` so malformed extractions surface before derive:
- missing named solicitation recipients
- missing bidder actor or date on substantive outcomes
- proxy-date export warnings
- lossy agreement-kind classification warnings

Then add focused gate and prompt-rendering tests.
  </action>
  <acceptance_criteria>
    - each hardening rule has direct test coverage
    - the prompt packet and gates agree on the strengthened contract
  </acceptance_criteria>
  <done>prompt/gate hardening is locked by direct tests</done>
</task>

</tasks>

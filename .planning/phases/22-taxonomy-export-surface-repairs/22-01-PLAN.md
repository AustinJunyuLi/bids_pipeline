---
phase: 22-taxonomy-export-surface-repairs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/models_v2.py
  - skill_pipeline/derive.py
  - skill_pipeline/db_export_v2.py
  - tests/test_skill_derive.py
  - tests/test_skill_db_export_v2.py
autonomous: true
requirements: [SURFACE-01, SURFACE-02, SURFACE-03, SURFACE-04]

must_haves:
  truths:
    - "agreement-family rows preserve additive analyst event types instead of collapsing everything to `nda`"
    - "advisor termination is representable on the analyst surface"
    - "proxy dates no longer populate exact `date_recorded` on analyst exports"
    - "enterprise-value-only proposals survive derive and export without being forced into per-share or range fields"
  artifacts:
    - path: "skill_pipeline/models_v2.py"
      provides: "expanded analyst row and process/event taxonomy contract"
    - path: "skill_pipeline/derive.py"
      provides: "agreement/process row mapping plus date/value metadata"
    - path: "skill_pipeline/db_export_v2.py"
      provides: "updated analyst/benchmark CSV surface"
    - path: "tests/test_skill_derive.py"
      provides: "taxonomy regression coverage"
    - path: "tests/test_skill_db_export_v2.py"
      provides: "export-surface regression coverage"
  key_links:
    - from: "skill_pipeline/derive.py"
      to: "skill_pipeline/db_export_v2.py"
      via: "enterprise value and date precision fields"
      pattern: "the derived row contract flows through to the live CSV surface"
    - from: "skill_pipeline/models_v2.py"
      to: "tests/test_skill_derive.py"
      via: "distinct analyst event types"
      pattern: "new taxonomy values stay pinned by direct row assertions"
---

<objective>
Repair taxonomy and export-surface fidelity.

Purpose: GPT Pro identified a set of row classes that cannot reconcile cleanly
because the current analyst surface collapses agreement families, lacks advisor
termination, leaks proxy dates as exact values, and drops enterprise-value-only
proposals. This plan fixes those additive surface defects without widening into
prompt or gate work.

Output: richer additive analyst event types, safer export date fields, and
enterprise-value support.
</objective>

<context>
@.planning/phases/22-taxonomy-export-surface-repairs/22-CONTEXT.md
@skill_pipeline/models_v2.py
@skill_pipeline/derive.py
@skill_pipeline/db_export_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the additive analyst row contract</name>
  <files>
    skill_pipeline/models_v2.py,
    skill_pipeline/derive.py,
    skill_pipeline/db_export_v2.py
  </files>
  <action>
Add the minimum additive model/runtime/export changes so:
- agreement families emit distinct analyst event types
- advisor termination is representable
- analyst rows preserve `date_precision` and `date_sort_proxy`
- proposal rows can carry `enterprise_value`
  </action>
  <acceptance_criteria>
    - agreement-family rows are no longer all `nda`
    - proxy dates do not populate exact `date_recorded`
    - enterprise-value-only proposals export with a dedicated field
  </acceptance_criteria>
  <done>the analyst-facing row/export contract matches the intended additive surface</done>
</task>

<task type="auto">
  <name>Task 2: Pin taxonomy and export behavior with tests</name>
  <files>
    tests/test_skill_derive.py,
    tests/test_skill_db_export_v2.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_derive.py tests/test_skill_db_export_v2.py -k 'agreement or exclusivity or amendment or enterprise or proxy or advisor_termination'</automated>
  </verify>
  <action>
Add regression tests that prove:
- agreement-family rows use the new event types
- advisor termination maps to `ib_terminated`
- proxy dates export through `date_sort_proxy` and not `date_recorded`
- enterprise-value-only proposals export their value surface cleanly
  </action>
  <acceptance_criteria>
    - tests assert the new row/event types directly
    - tests assert the analyst CSV fields and values directly
  </acceptance_criteria>
  <done>the new surface contract is locked by derive/export regression coverage</done>
</task>

</tasks>

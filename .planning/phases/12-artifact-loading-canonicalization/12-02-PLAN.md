---
phase: 12-artifact-loading-canonicalization
plan: 02
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - skill_pipeline/canonicalize.py
  - skill_pipeline/cli.py
  - tests/test_skill_canonicalize_v2.py
  - tests/test_skill_pipeline.py
  - CLAUDE.md
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "`canonicalize-v2` upgrades quote-first observation artifacts into canonical span-backed artifacts using the shared span resolver"
    - "The CLI exposes `canonicalize-v2` as an additive command"
    - "Authoritative repo memory reflects that `canonicalize-v2` is now a live writer for canonical v2 observation outputs"
  artifacts:
    - path: "skill_pipeline/canonicalize.py"
      provides: "v2 canonicalization path"
    - path: "skill_pipeline/cli.py"
      provides: "CLI registration for canonicalize-v2"
    - path: "tests/test_skill_canonicalize_v2.py"
      provides: "Synthetic end-to-end v2 canonicalization tests"
    - path: "tests/test_skill_pipeline.py"
      provides: "CLI parser regression for canonicalize-v2"
    - path: "CLAUDE.md"
      provides: "Updated repo memory for live v2 canonicalization outputs"
  key_links:
    - from: "skill_pipeline/canonicalize.py"
      to: "tests/test_skill_canonicalize_v2.py"
      via: "quote-first observation fixtures resolved to spans"
      pattern: "shared span resolver reused without inheriting v1 event heuristics"
---

<objective>
Make the v2 canonicalization path runnable end-to-end on synthetic fixtures.

Purpose: Later validation and derivation phases need canonical span-backed v2
observations, and the repo needs a real command that produces them now.

Output: `canonicalize-v2` command, v2 canonical outputs on disk, fixture tests,
and updated repo memory.
</objective>

<context>
@.planning/phases/12-artifact-loading-canonicalization/12-CONTEXT.md
@.planning/phases/12-artifact-loading-canonicalization/12-RESEARCH.md
@skill_pipeline/canonicalize.py
@skill_pipeline/cli.py
@skill_pipeline/models_v2.py
@skill_pipeline/extract_artifacts_v2.py
@tests/test_skill_canonicalize.py
@tests/test_skill_pipeline.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the v2 canonicalization path</name>
  <files>
    skill_pipeline/canonicalize.py,
    skill_pipeline/cli.py
  </files>
  <action>
Add `run_canonicalize_v2` that:
- loads quote-first v2 observation artifacts
- resolves quotes to spans using the shared `_resolve_quotes_to_spans()` path
- writes canonical `observations.json` and `extract_v2/spans.json`
- leaves v1 canonicalization behavior unchanged

Register the additive `canonicalize-v2` CLI command.
  </action>
  <acceptance_criteria>
    - quote-first v2 fixtures are upgraded to canonical artifacts
    - canonical v2 fixtures can be re-read without span drift
    - CLI dispatch reaches the new v2 runtime
  </acceptance_criteria>
  <done>`canonicalize-v2` exists and produces canonical v2 artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Add canonicalize-v2 tests and update memory</name>
  <files>
    tests/test_skill_canonicalize_v2.py,
    tests/test_skill_pipeline.py,
    CLAUDE.md
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_canonicalize_v2.py tests/test_skill_pipeline.py -k 'canonicalize_v2 or canonicalize-v2 or canonicalize_v2'</automated>
  </verify>
  <acceptance_criteria>
    - tests prove spans are written and referenced by parties/cohorts/observations
    - parser tests cover `canonicalize-v2`
    - CLAUDE.md distinguishes live Phase 12 canonical outputs from still-reserved future v2 surfaces
  </acceptance_criteria>
  <done>Phase 12 runtime path is tested and documented</done>
</task>

</tasks>

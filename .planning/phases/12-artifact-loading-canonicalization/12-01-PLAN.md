---
phase: 12-artifact-loading-canonicalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/extract_artifacts_v2.py
  - tests/test_skill_extract_artifacts_v2.py
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "A separate v2 loader can load quote-first or canonical observation artifacts without modifying the v1 loader"
    - "The repo can distinguish v1-only, v2-only, both, and empty extract surfaces on disk"
  artifacts:
    - path: "skill_pipeline/extract_artifacts_v2.py"
      provides: "Typed v2 artifact loading and version routing"
    - path: "tests/test_skill_extract_artifacts_v2.py"
      provides: "Regression tests for v2 load modes and v1/v2 detection"
  key_links:
    - from: "skill_pipeline/extract_artifacts_v2.py"
      to: "tests/test_skill_extract_artifacts_v2.py"
      via: "Synthetic raw/canonical payload fixtures"
      pattern: "Loader behavior is proven by direct typed loads and ambiguity checks"
---

<objective>
Create the v2 loader surface and the smallest safe version-routing helper.

Purpose: Downstream v2 phases need typed loading now, but the v1 loader must
remain untouched and callers must not silently load the wrong contract when
both families exist.

Output: `extract_artifacts_v2.py` plus dedicated loader tests.
</objective>

<context>
@.planning/phases/12-artifact-loading-canonicalization/12-CONTEXT.md
@.planning/phases/12-artifact-loading-canonicalization/12-RESEARCH.md
@skill_pipeline/extract_artifacts.py
@skill_pipeline/models_v2.py
@skill_pipeline/models.py
@tests/test_skill_extract_artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed v2 loaders and version detection</name>
  <files>
    skill_pipeline/extract_artifacts_v2.py
  </files>
  <action>
Create a separate v2 loader module with:
- quote-first v2 wrapper models for `observations_raw.json`
- `LoadedObservationArtifacts`
- a loader that supports `quote_first` and `canonical` modes
- a version router or detector that distinguishes `v1`, `v2`, `both`, and `none`
without touching the existing v1 loader behavior
  </action>
  <acceptance_criteria>
    - canonical v2 loading requires `spans_v2_path`
    - quote-first v2 loading returns typed raw observation artifacts
    - ambiguous `both` state does not silently pick a contract
  </acceptance_criteria>
  <done>v2 artifacts can be loaded safely beside v1 artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Add loader regression tests</name>
  <files>
    tests/test_skill_extract_artifacts_v2.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_extract_artifacts_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - tests cover quote-first and canonical v2 loads
    - tests cover missing spans failure
    - tests cover v1/v2/both/none detection behavior
  </acceptance_criteria>
  <done>Phase 12 loader surface is pinned by synthetic fixture tests</done>
</task>

</tasks>

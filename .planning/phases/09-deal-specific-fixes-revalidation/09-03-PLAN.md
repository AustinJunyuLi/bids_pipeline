---
phase: 09-deal-specific-fixes-revalidation
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - data/skill/zep/reconcile/reconciliation_report.json
  - data/skill/medivation/reconcile/reconciliation_report.json
  - data/reconciliation_cross_deal_analysis.md
autonomous: true
requirements: [RERUN-02]

must_haves:
  truths:
    - "9-deal reconciliation shows a higher atomic match rate than the 2026-03-29 baseline of 70.3% (156/222)"
    - "9-deal reconciliation shows fewer filing-contradicted pipeline claims than the baseline of 16"
    - "Improvement is auditable with per-deal deltas between baseline and refreshed numbers"
  artifacts:
    - path: "data/skill/zep/reconcile/reconciliation_report.json"
      provides: "Refreshed Zep reconciliation with filing arbitration"
    - path: "data/skill/medivation/reconcile/reconciliation_report.json"
      provides: "Refreshed Medivation reconciliation with filing arbitration"
    - path: "data/reconciliation_cross_deal_analysis.md"
      provides: "Refreshed 9-deal cross-deal analysis with baseline comparison"
  key_links:
    - from: "data/skill/zep/export/deal_events.csv"
      to: "data/skill/zep/reconcile/reconciliation_report.json"
      via: "/reconcile-alex zep"
      pattern: "Post-export reconciliation"
    - from: "data/skill/medivation/export/deal_events.csv"
      to: "data/skill/medivation/reconcile/reconciliation_report.json"
      via: "/reconcile-alex medivation"
      pattern: "Post-export reconciliation"
    - from: "data/skill/*/reconcile/reconciliation_report.json"
      to: "data/reconciliation_cross_deal_analysis.md"
      via: "Cross-deal summary aggregation"
      pattern: "All 9 per-deal reports aggregated"
---

<objective>
Run post-export reconciliation for both affected deals and regenerate the 9-deal cross-deal analysis to measure improvement against the 2026-03-29 baseline (RERUN-02).

Purpose: Per D-12, post-export validation is corpus-wide. After Zep and Medivation pass db-export (Plans 01 and 02), the reconciliation comparison shows whether the re-extraction improved the atomic match rate and reduced filing-contradicted pipeline claims. The 7 frozen deals keep their existing reconciliation reports; only Zep and Medivation are refreshed. The cross-deal analysis is regenerated from all 9 per-deal reports.

Output: Refreshed reconciliation reports for Zep and Medivation, and a regenerated cross-deal analysis document with explicit baseline-vs-rerun comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-CONTEXT.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-01-SUMMARY.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-02-SUMMARY.md
@.claude/skills/reconcile-alex/SKILL.md
@data/reconciliation_cross_deal_analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run per-deal reconciliation for Zep and Medivation</name>
  <files>
    data/skill/zep/reconcile/reconciliation_report.json,
    data/skill/medivation/reconcile/reconciliation_report.json
  </files>
  <read_first>
    .claude/skills/reconcile-alex/SKILL.md,
    data/skill/zep/export/deal_events.csv,
    data/skill/medivation/export/deal_events.csv,
    data/skill/zep/extract/actors_raw.json,
    data/skill/zep/extract/events_raw.json,
    data/skill/medivation/extract/actors_raw.json,
    data/skill/medivation/extract/events_raw.json
  </read_first>
  <action>
Per D-12 (post-export corpus-wide validation) and D-13 (benchmark boundary now
lifted because db-export completed for both deals):

1. Run `/reconcile-alex zep` following the reconcile-alex SKILL.md workflow:
   - Prerequisites: confirm data/skill/zep/export/deal_events.csv exists,
     data/skill/zep/extract/actors_raw.json exists,
     data/skill/zep/extract/events_raw.json exists,
     data/skill/zep/extract/spans.json exists,
     data/skill/zep/enrich/deterministic_enrichment.json exists
   - Read the benchmark spreadsheet data from example/ for Zep
   - Read the pipeline export CSV and extraction artifacts
   - Perform row matching, disagreement arbitration against filing text
   - Write data/skill/zep/reconcile/reconciliation_report.json

2. Run `/reconcile-alex medivation` following the same workflow:
   - Prerequisites: confirm the same artifact set exists for medivation
   - Write data/skill/medivation/reconcile/reconciliation_report.json

3. Record the per-deal comparison against the 2026-03-29 baseline:
   - Zep baseline: 14 matched, 1 pipeline-only, 9 alex-only, 16 pipeline events, 23 alex rows
   - Medivation baseline: 12 matched, 19 pipeline-only, 2 alex-only, 33 pipeline events, 16 alex rows
   - Note the delta for each metric in the reconciliation report
  </action>
  <verify>
    <automated>python -c "
import json, pathlib
for slug in ['zep', 'medivation']:
    p = pathlib.Path(f'data/skill/{slug}/reconcile/reconciliation_report.json')
    assert p.exists(), f'Missing: {p}'
    with open(p) as f:
        r = json.load(f)
    assert isinstance(r, dict), f'{slug} report is not a dict'
    print(f'{slug} reconciliation report exists ({p.stat().st_size} bytes)')
print('Per-deal reconciliation PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/skill/zep/reconcile/reconciliation_report.json exists and is valid JSON
    - data/skill/medivation/reconcile/reconciliation_report.json exists and is valid JSON
    - Both reports were generated after db-export completed (benchmark boundary respected)
    - Reports contain match counts, pipeline-only counts, alex-only counts, and arbitration results
  </acceptance_criteria>
  <done>Per-deal reconciliation reports regenerated for Zep and Medivation with filing arbitration</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate 9-deal cross-deal analysis with baseline comparison</name>
  <files>
    data/reconciliation_cross_deal_analysis.md
  </files>
  <read_first>
    data/reconciliation_cross_deal_analysis.md,
    data/skill/zep/reconcile/reconciliation_report.json,
    data/skill/medivation/reconcile/reconciliation_report.json,
    data/skill/imprivata/reconcile/reconciliation_report.json,
    data/skill/mac-gray/reconcile/reconciliation_report.json,
    data/skill/penford/reconcile/reconciliation_report.json,
    data/skill/petsmart-inc/reconcile/reconciliation_report.json,
    data/skill/providence-worcester/reconcile/reconciliation_report.json,
    data/skill/saks/reconcile/reconciliation_report.json,
    data/skill/stec/reconcile/reconciliation_report.json
  </read_first>
  <action>
Regenerate data/reconciliation_cross_deal_analysis.md by reading all 9 per-deal
reconciliation reports and producing an updated cross-deal summary.

1. Read all 9 per-deal reconciliation reports from
   data/skill/{slug}/reconcile/reconciliation_report.json for each slug:
   imprivata, mac-gray, medivation, penford, petsmart-inc,
   providence-worcester, saks, stec, zep.

2. Extract per-deal metrics: pipeline event count, alex row count, matched
   pairs, pipeline-only, alex-only, aggregate rows, pipeline-wins
   (arbitration), alex-wins (arbitration).

3. Compute corpus-wide totals by summing per-deal metrics.

4. Calculate the atomic match rate: matched / (alex rows - aggregate rows).

5. Write data/reconciliation_cross_deal_analysis.md with:
   - Header: "# Cross-Deal Reconciliation Analysis: Pipeline vs. Alex"
   - Generated date: current date
   - Per-deal summary table with all metrics
   - Corpus-wide totals row
   - A "## Baseline Comparison (v1.1 Phase 9)" section containing:
     * 2026-03-29 baseline numbers: 156 matched, 45 pipeline-wins, 16 alex-wins,
       70.3% atomic match rate (156/222 alex atomic rows)
     * New refreshed numbers (from step 3-4)
     * Delta table showing change per metric
     * Per-deal deltas for Zep and Medivation specifically
     * The 7 frozen deals should show identical numbers; flag if any differ

6. Per D-12, explicitly note whether the new atomic match rate exceeds 70.3%
   and whether filing-contradicted pipeline claims (alex-wins) dropped below 16.
   If improvement did not materialize, document the actual numbers and describe
   what changed. Do not fabricate improvement.

IMPORTANT: This is a measurement step. Report the actual numbers truthfully.
The acceptance gate is measurable improvement, but if the re-extraction
produced different (not necessarily better) results, document that honestly.
  </action>
  <verify>
    <automated>python -c "
import pathlib
p = pathlib.Path('data/reconciliation_cross_deal_analysis.md')
assert p.exists(), 'Missing cross-deal analysis'
text = p.read_text()
assert 'Baseline Comparison' in text, 'Missing baseline comparison section'
assert '2026-03-29' in text, 'Missing baseline date reference'
assert 'atomic match rate' in text.lower() or 'match rate' in text.lower(), 'Missing match rate metric'
lines = text.strip().split('\n')
assert len(lines) >= 20, f'Cross-deal analysis too short: {len(lines)} lines'
print(f'Cross-deal analysis PASSED: {len(lines)} lines, has baseline comparison')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/reconciliation_cross_deal_analysis.md exists and has >= 20 lines
    - File contains "Baseline Comparison" section header
    - File references "2026-03-29" baseline date
    - File contains atomic match rate metric (old vs new)
    - File contains per-deal summary table covering all 9 deals
    - File contains corpus-wide totals
  </acceptance_criteria>
  <done>Cross-deal analysis regenerated with explicit baseline comparison showing whether Phase 9 re-extractions improved reconciliation metrics</done>
</task>

</tasks>

<verification>
1. RERUN-02: Atomic match rate compared against 70.3% baseline
2. RERUN-02: Filing-contradicted pipeline claims compared against 16 baseline
3. Both affected deals have refreshed reconciliation reports
4. Cross-deal analysis regenerated with explicit baseline comparison
5. 7 frozen deals show unchanged reconciliation numbers
</verification>

<success_criteria>
- 9-deal atomic match rate > 70.3% (baseline: 156/222)
- Filing-contradicted pipeline claims (alex-wins) < 16 (baseline)
- Cross-deal analysis document has explicit baseline-vs-rerun comparison section
- Improvement is traceable to Zep NMC fix and Medivation consistency fix
</success_criteria>

<output>
After completion, create `.planning/phases/09-deal-specific-fixes-revalidation/09-03-SUMMARY.md`
</output>

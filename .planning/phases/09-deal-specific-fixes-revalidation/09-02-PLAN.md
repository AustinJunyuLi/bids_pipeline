---
phase: 09-deal-specific-fixes-revalidation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - data/skill/medivation/extract/actors_raw.json
  - data/skill/medivation/extract/events_raw.json
  - data/skill/medivation/extract/spans.json
  - data/skill/medivation/canonicalize/canonicalize_log.json
  - data/skill/medivation/check/check_report.json
  - data/skill/medivation/verify/verification_findings.json
  - data/skill/medivation/verify/verification_log.json
  - data/skill/medivation/coverage/coverage_findings.json
  - data/skill/medivation/coverage/coverage_summary.json
  - data/skill/medivation/gates/gates_report.json
  - data/skill/medivation/enrich/deterministic_enrichment.json
  - data/skill/medivation/export/deal_events.csv
  - data/skill/medivation/prompt/manifest.json
autonomous: true
requirements: [EXTRACT-05, RERUN-01]

must_haves:
  truths:
    - "Medivation coverage_notes do not reference event IDs absent from the events array"
    - "Medivation passes full deterministic pipeline (canonicalize through db-export) without errors"
    - "Medivation deal_events.csv is regenerated from DuckDB with internally consistent extraction data"
    - "Medivation bidder-round chronology is preserved with Company 1-4 proposals and round milestones intact"
  artifacts:
    - path: "data/skill/medivation/extract/events_raw.json"
      provides: "Corrected Medivation events with consistent coverage_notes references"
    - path: "data/skill/medivation/extract/actors_raw.json"
      provides: "Regenerated Medivation actor roster"
    - path: "data/skill/medivation/extract/spans.json"
      provides: "Canonical span registry for Medivation"
    - path: "data/skill/medivation/export/deal_events.csv"
      provides: "DuckDB-exported Medivation event CSV"
    - path: "data/skill/medivation/enrich/deterministic_enrichment.json"
      provides: "Deterministic enrichment with DropTarget and all_cash"
  key_links:
    - from: "data/skill/medivation/extract/events_raw.json"
      to: "data/skill/medivation/export/deal_events.csv"
      via: "canonicalize -> check -> verify -> coverage -> gates -> enrich-core -> db-load -> db-export"
      pattern: "Full deterministic pipeline chain"
---

<objective>
Re-extract Medivation with Phase 8-updated skill docs to fix the coverage_notes/events array mismatch (EXTRACT-05), then run the full deterministic pipeline through db-export (RERUN-01 partial).

Purpose: The current Medivation extraction has a coverage_notes field citing drop events `evt_027` and `evt_029` that do not exist in the events array. Per D-07 and D-08, the real defect is this internal inconsistency, not missing proposal events (evt_013 and evt_017 already exist). A clean re-extraction regenerates both events and coverage_notes from scratch, resolving the mismatch. Per D-09, the bidder-round chronology (Company 1-4 proposals, round milestones, extension-round events) must be preserved.

Output: Regenerated Medivation extraction artifacts, full deterministic pipeline artifacts, and DuckDB-exported deal_events.csv.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-CONTEXT.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-RESEARCH.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-01-SUMMARY.md
@.claude/skills/extract-deal/SKILL.md
@.claude/skills/verify-extraction/SKILL.md
@.claude/skills/deal-agent/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Medivation downstream artifacts and re-extract</name>
  <files>
    data/skill/medivation/extract/actors_raw.json,
    data/skill/medivation/extract/events_raw.json,
    data/skill/medivation/prompt/manifest.json
  </files>
  <read_first>
    .claude/skills/extract-deal/SKILL.md,
    .claude/skills/deal-agent/SKILL.md,
    data/deals/medivation/source/chronology_selection.json,
    data/deals/medivation/source/chronology_blocks.jsonl (first 5 lines for structure),
    raw/medivation/document_registry.json
  </read_first>
  <action>
Per D-10 (targeted extraction-forward rerun) and D-02 (use Phase 8 guidance as-is):

1. Delete downstream artifacts (preserve raw/ and source/):
   ```
   rm -rf data/skill/medivation/extract
   rm -rf data/skill/medivation/canonicalize
   rm -rf data/skill/medivation/check
   rm -rf data/skill/medivation/verify
   rm -rf data/skill/medivation/coverage
   rm -rf data/skill/medivation/gates
   rm -rf data/skill/medivation/enrich
   rm -rf data/skill/medivation/export
   rm -rf data/skill/medivation/prompt
   ```

2. Recreate empty directories:
   ```
   mkdir -p data/skill/medivation/{extract,canonicalize,check,verify,coverage,gates,enrich,export,prompt}
   ```

3. Run `skill-pipeline compose-prompts --deal medivation --mode actors` to generate actor prompt packets.

4. Execute `/extract-deal medivation` following the canonical SKILL.md workflow:
   - Read prompt packets from `data/skill/medivation/prompt/packets/*/rendered.md`
   - Extract actors first, write `data/skill/medivation/extract/actors_raw.json`
   - Then run `skill-pipeline compose-prompts --deal medivation --mode events`
   - Extract events, write `data/skill/medivation/extract/events_raw.json`

5. CRITICAL per D-08: The concrete defect is coverage_notes citing events
   (evt_027, evt_029) that do not exist in the events array. A clean
   re-extraction resolves this by regenerating both events and coverage_notes
   from the filing text. Ensure internal consistency: every event ID
   referenced in coverage_notes must exist in the events array.

6. CRITICAL per D-09: Preserve the bidder-round chronology. Medivation has a
   multi-round process with Company 1-4 proposals, round milestones (July
   19-20 process letters, August 10 round-selection meeting, August 14
   instruction letter, August 19 deadline, August 20 extension), and
   extension-round events. Do not collapse these into coarser groupings.

7. BENCHMARK BOUNDARY per D-13: Do NOT read data/skill/medivation/reconcile/,
   example/, diagnosis/, or any benchmark material during extraction.
  </action>
  <verify>
    <automated>python -c "
import json, re
with open('data/skill/medivation/extract/actors_raw.json') as f: a = json.load(f)
with open('data/skill/medivation/extract/events_raw.json') as f: e = json.load(f)
assert 'actors' in a, 'actors key missing'
assert 'events' in e, 'events key missing'
assert len(a['actors']) >= 3, f'too few actors: {len(a[\"actors\"])}'
assert len(e['events']) >= 15, f'too few events: {len(e[\"events\"])}'
# EXTRACT-05 gate: coverage_notes consistency
event_ids = {ev['event_id'] for ev in e['events']}
for note in e.get('coverage_notes', []):
    for ref in re.findall(r'evt_\d+', note):
        assert ref in event_ids, f'coverage_notes references {ref} not in events array'
print(f'EXTRACT-05 gate PASSED: {len(e[\"events\"])} events, coverage_notes consistent')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/skill/medivation/extract/actors_raw.json exists and contains "actors" key with >= 3 actors
    - data/skill/medivation/extract/events_raw.json exists and contains "events" key with >= 15 events
    - Every evt_NNN reference in coverage_notes exists in the events array
    - data/skill/medivation/prompt/manifest.json exists
    - data/deals/medivation/source/ and raw/medivation/ directories are untouched
  </acceptance_criteria>
  <done>Medivation re-extracted with internally consistent coverage_notes and preserved bidder-round chronology per D-08 and D-09</done>
</task>

<task type="auto">
  <name>Task 2: Run Medivation full deterministic pipeline through db-export</name>
  <files>
    data/skill/medivation/extract/spans.json,
    data/skill/medivation/canonicalize/canonicalize_log.json,
    data/skill/medivation/check/check_report.json,
    data/skill/medivation/verify/verification_findings.json,
    data/skill/medivation/verify/verification_log.json,
    data/skill/medivation/coverage/coverage_findings.json,
    data/skill/medivation/coverage/coverage_summary.json,
    data/skill/medivation/gates/gates_report.json,
    data/skill/medivation/enrich/deterministic_enrichment.json,
    data/skill/medivation/export/deal_events.csv
  </files>
  <read_first>
    data/skill/medivation/extract/actors_raw.json,
    data/skill/medivation/extract/events_raw.json,
    .claude/skills/verify-extraction/SKILL.md,
    .claude/skills/deal-agent/SKILL.md
  </read_first>
  <action>
Run the full deterministic pipeline for Medivation per D-10 and the deal-agent stage ordering:

1. `skill-pipeline canonicalize --deal medivation`
   - Expect: spans.json created, actors_raw.json and events_raw.json upgraded to canonical form

2. `skill-pipeline check --deal medivation`
   - Expect: check_report.json with no blockers.

3. `skill-pipeline verify --deal medivation`
   - Expect: verification_findings.json. Only EXACT and NORMALIZED pass; FUZZY does not.

4. `skill-pipeline coverage --deal medivation`
   - Expect: coverage_findings.json and coverage_summary.json.

5. `skill-pipeline gates --deal medivation`
   - Expect: gates_report.json with no blocker findings.

6. If verify or coverage findings contain repairable errors, run `/verify-extraction medivation`
   per the verify-extraction SKILL.md (max 2 rounds). Then re-run steps 2-5 to confirm fixes.
   Per D-11, prefer this repair loop over hand-editing.

7. `skill-pipeline enrich-core --deal medivation`
   - Expect: deterministic_enrichment.json with bid classifications, rounds, cycles,
     dropout_classifications, and all_cash_overrides.
   - Pre-gate: gates must have no blocker findings before enrich-core runs.

7a. `/enrich-deal medivation` (mandatory interpretive enrichment)
   - Reads deterministic_enrichment.json as baseline.
   - Writes enrichment.json with interpretive dropout labels (non-DropTarget),
     initiation judgment, advisory verification, count reconciliation, review_flags.
   - Gate: enrichment.json exists and contains 5 required keys.

8. `skill-pipeline db-load --deal medivation`
   - Expect: data/pipeline.duckdb updated with Medivation deal data.

9. `skill-pipeline db-export --deal medivation`
   - Expect: data/skill/medivation/export/deal_events.csv generated from DuckDB queries.

BENCHMARK BOUNDARY per D-13: Do NOT read reconciliation or benchmark materials
until after db-export completes.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib
base = pathlib.Path('data/skill/medivation')
# All required artifacts exist
for sub in ['extract/spans.json', 'canonicalize/canonicalize_log.json',
            'check/check_report.json', 'verify/verification_findings.json',
            'coverage/coverage_findings.json', 'gates/gates_report.json',
            'enrich/deterministic_enrichment.json', 'export/deal_events.csv']:
    p = base / sub
    assert p.exists(), f'Missing: {p}'
# Gates has no blockers
with open(base / 'gates/gates_report.json') as f:
    gr = json.load(f)
    blockers = [f for f in gr.get('findings', []) if f.get('severity') == 'blocker']
    assert len(blockers) == 0, f'Gates has {len(blockers)} blocker(s)'
# Export CSV is non-empty
csv_path = base / 'export/deal_events.csv'
lines = csv_path.read_text().strip().split('\n')
assert len(lines) >= 2, f'deal_events.csv has only {len(lines)} lines'
print(f'Medivation pipeline PASSED: {len(lines)-1} event rows exported')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/skill/medivation/extract/spans.json exists (canonical form)
    - data/skill/medivation/canonicalize/canonicalize_log.json exists
    - data/skill/medivation/check/check_report.json exists with no blockers
    - data/skill/medivation/verify/verification_findings.json exists
    - data/skill/medivation/coverage/coverage_findings.json exists
    - data/skill/medivation/gates/gates_report.json exists with zero blocker findings
    - data/skill/medivation/enrich/deterministic_enrichment.json exists
    - data/skill/medivation/export/deal_events.csv exists with >= 2 lines (header + data)
    - data/pipeline.duckdb has been updated with Medivation data
  </acceptance_criteria>
  <done>Medivation passes full deterministic pipeline from canonicalize through db-export with no blocker findings</done>
</task>

</tasks>

<verification>
1. EXTRACT-05: Every event ID referenced in coverage_notes exists in the events array
2. RERUN-01 (partial): All deterministic pipeline stages complete without errors for Medivation
3. All artifact files exist at expected paths under data/skill/medivation/
4. deal_events.csv is generated from DuckDB (not hand-crafted)
5. Bidder-round chronology preserved per D-09
</verification>

<success_criteria>
- Medivation coverage_notes references only event IDs present in the events array (EXTRACT-05)
- All 8 deterministic stages (canonicalize, check, verify, coverage, gates, enrich-core, db-load, db-export) pass without errors
- deal_events.csv contains event rows with consistent actor/event attribution
- Bidder-round chronology is not collapsed (multiple Company proposals and round milestones remain)
</success_criteria>

<output>
After completion, create `.planning/phases/09-deal-specific-fixes-revalidation/09-02-SUMMARY.md`
</output>

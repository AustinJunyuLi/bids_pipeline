---
phase: 09-deal-specific-fixes-revalidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/skill/zep/extract/actors_raw.json
  - data/skill/zep/extract/events_raw.json
  - data/skill/zep/extract/spans.json
  - data/skill/zep/canonicalize/canonicalize_log.json
  - data/skill/zep/check/check_report.json
  - data/skill/zep/verify/verification_findings.json
  - data/skill/zep/verify/verification_log.json
  - data/skill/zep/coverage/coverage_findings.json
  - data/skill/zep/coverage/coverage_summary.json
  - data/skill/zep/gates/gates_report.json
  - data/skill/zep/enrich/deterministic_enrichment.json
  - data/skill/zep/export/deal_events.csv
  - data/skill/zep/prompt/manifest.json
autonomous: true
requirements: [EXTRACT-04, RERUN-01]

must_haves:
  truths:
    - "Zep extraction no longer includes bidder_new_mountain_capital in any grouped 2014-cycle proposal or drop event actor_ids"
    - "Zep passes full deterministic pipeline (canonicalize through db-export) without errors"
    - "Zep deal_events.csv is regenerated from DuckDB with corrected extraction data"
  artifacts:
    - path: "data/skill/zep/extract/events_raw.json"
      provides: "Corrected Zep events without NMC contamination in 2014 grouped events"
    - path: "data/skill/zep/extract/actors_raw.json"
      provides: "Regenerated Zep actor roster"
    - path: "data/skill/zep/extract/spans.json"
      provides: "Canonical span registry for Zep"
    - path: "data/skill/zep/export/deal_events.csv"
      provides: "DuckDB-exported Zep event CSV"
    - path: "data/skill/zep/enrich/deterministic_enrichment.json"
      provides: "Deterministic enrichment with DropTarget and all_cash"
  key_links:
    - from: "data/skill/zep/extract/events_raw.json"
      to: "data/skill/zep/export/deal_events.csv"
      via: "canonicalize -> check -> verify -> coverage -> gates -> enrich-core -> db-load -> db-export"
      pattern: "Full deterministic pipeline chain"
---

<objective>
Re-extract Zep with Phase 8-updated skill docs to fix NMC actor contamination (EXTRACT-04), then run the full deterministic pipeline through db-export (RERUN-01 partial).

Purpose: The current Zep extraction incorrectly includes `bidder_new_mountain_capital` in 2014-cycle grouped proposal and drop events. NMC signed an NDA but declined to submit an IOI. A clean re-extraction with the Phase 8 guidance will naturally exclude NMC from these grouped events. Per D-04, this is the primary acceptance gate. Per D-06, any additional filing-grounded improvements from the updated guidance are welcome byproducts.

Output: Regenerated Zep extraction artifacts, full deterministic pipeline artifacts, and DuckDB-exported deal_events.csv.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-CONTEXT.md
@.planning/phases/09-deal-specific-fixes-revalidation/09-RESEARCH.md
@.claude/skills/extract-deal/SKILL.md
@.claude/skills/verify-extraction/SKILL.md
@.claude/skills/deal-agent/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Zep downstream artifacts and re-extract</name>
  <files>
    data/skill/zep/extract/actors_raw.json,
    data/skill/zep/extract/events_raw.json,
    data/skill/zep/prompt/manifest.json
  </files>
  <read_first>
    .claude/skills/extract-deal/SKILL.md,
    .claude/skills/deal-agent/SKILL.md,
    data/deals/zep/source/chronology_selection.json,
    data/deals/zep/source/chronology_blocks.jsonl (first 5 lines for structure),
    raw/zep/document_registry.json
  </read_first>
  <action>
Per D-10 (targeted extraction-forward rerun) and D-02 (use Phase 8 guidance as-is):

1. Delete downstream artifacts (preserve raw/ and source/):
   ```
   rm -rf data/skill/zep/extract
   rm -rf data/skill/zep/canonicalize
   rm -rf data/skill/zep/check
   rm -rf data/skill/zep/verify
   rm -rf data/skill/zep/coverage
   rm -rf data/skill/zep/gates
   rm -rf data/skill/zep/enrich
   rm -rf data/skill/zep/export
   rm -rf data/skill/zep/prompt
   ```

2. Recreate empty directories:
   ```
   mkdir -p data/skill/zep/{extract,canonicalize,check,verify,coverage,gates,enrich,export,prompt}
   ```

3. Run `skill-pipeline compose-prompts --deal zep --mode actors` to generate actor prompt packets.

4. Execute `/extract-deal zep` following the canonical SKILL.md workflow:
   - Read prompt packets from `data/skill/zep/prompt/packets/*/rendered.md`
   - Extract actors first, write `data/skill/zep/extract/actors_raw.json`
   - Then run `skill-pipeline compose-prompts --deal zep --mode events`
   - Extract events, write `data/skill/zep/extract/events_raw.json`

5. CRITICAL per D-04: During extraction, NMC signed an NDA (early 2014) but
   declined to submit an IOI. In the 2014 cycle grouped proposal and grouped
   drop events, do NOT include `bidder_new_mountain_capital` in actor_ids.
   NMC's active bidding participation only begins in the 2015 cycle (after the
   process restarted). The Phase 8 guidance on NDA exclusions and round
   milestones should help produce correct groupings naturally.

6. BENCHMARK BOUNDARY per D-13: Do NOT read data/skill/zep/reconcile/,
   example/, diagnosis/, or any benchmark material during extraction.
  </action>
  <verify>
    <automated>python -c "
import json
with open('data/skill/zep/extract/actors_raw.json') as f: a = json.load(f)
with open('data/skill/zep/extract/events_raw.json') as f: e = json.load(f)
assert 'actors' in a, 'actors key missing'
assert 'events' in e, 'events key missing'
assert len(a['actors']) >= 3, f'too few actors: {len(a[\"actors\"])}'
assert len(e['events']) >= 10, f'too few events: {len(e[\"events\"])}'
# EXTRACT-04 gate: no NMC in 2014-cycle grouped events
for ev in e['events']:
    if ev.get('event_type') in ('proposal', 'drop'):
        aids = ev.get('actor_ids', [])
        if len(aids) > 1 and 'bidder_new_mountain_capital' in aids:
            # Check if this is a 2014 event (before restart)
            d = ev.get('event_date', '')
            if d.startswith('2014'):
                raise AssertionError(f'NMC in grouped 2014 event {ev[\"event_id\"]}: {aids}')
print('EXTRACT-04 gate PASSED: no NMC in grouped 2014-cycle events')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/skill/zep/extract/actors_raw.json exists and contains "actors" key with >= 3 actors
    - data/skill/zep/extract/events_raw.json exists and contains "events" key with >= 10 events
    - No 2014-dated grouped proposal or drop event contains "bidder_new_mountain_capital" in actor_ids
    - data/skill/zep/prompt/manifest.json exists
    - data/deals/zep/source/ and raw/zep/ directories are untouched
  </acceptance_criteria>
  <done>Zep re-extracted with NMC excluded from 2014-cycle grouped events per D-04</done>
</task>

<task type="auto">
  <name>Task 2: Run Zep full deterministic pipeline through db-export</name>
  <files>
    data/skill/zep/extract/spans.json,
    data/skill/zep/canonicalize/canonicalize_log.json,
    data/skill/zep/check/check_report.json,
    data/skill/zep/verify/verification_findings.json,
    data/skill/zep/verify/verification_log.json,
    data/skill/zep/coverage/coverage_findings.json,
    data/skill/zep/coverage/coverage_summary.json,
    data/skill/zep/gates/gates_report.json,
    data/skill/zep/enrich/deterministic_enrichment.json,
    data/skill/zep/export/deal_events.csv
  </files>
  <read_first>
    data/skill/zep/extract/actors_raw.json,
    data/skill/zep/extract/events_raw.json,
    .claude/skills/verify-extraction/SKILL.md,
    .claude/skills/deal-agent/SKILL.md
  </read_first>
  <action>
Run the full deterministic pipeline for Zep per D-10 and the deal-agent stage ordering:

1. `skill-pipeline canonicalize --deal zep`
   - Expect: spans.json created, actors_raw.json and events_raw.json upgraded to canonical form

2. `skill-pipeline check --deal zep`
   - Expect: check_report.json with no blockers. If blockers, address per check rules.

3. `skill-pipeline verify --deal zep`
   - Expect: verification_findings.json. Only EXACT and NORMALIZED pass; FUZZY does not.

4. `skill-pipeline coverage --deal zep`
   - Expect: coverage_findings.json and coverage_summary.json.

5. `skill-pipeline gates --deal zep`
   - Expect: gates_report.json with no blocker findings.

6. If verify or coverage findings contain repairable errors, run `/verify-extraction zep`
   per the verify-extraction SKILL.md (max 2 rounds). Then re-run steps 2-5 to confirm fixes.
   Per D-11, prefer this repair loop over hand-editing.

7. `skill-pipeline enrich-core --deal zep`
   - Expect: deterministic_enrichment.json with bid classifications, rounds, cycles,
     dropout_classifications, and all_cash_overrides.
   - Pre-gate: gates must have no blocker findings before enrich-core runs.

8. `skill-pipeline db-load --deal zep`
   - Expect: data/pipeline.duckdb updated with Zep deal data.

9. `skill-pipeline db-export --deal zep`
   - Expect: data/skill/zep/export/deal_events.csv generated from DuckDB queries.

BENCHMARK BOUNDARY per D-13: Do NOT read reconciliation or benchmark materials
until after db-export completes.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib
base = pathlib.Path('data/skill/zep')
# All required artifacts exist
for sub in ['extract/spans.json', 'canonicalize/canonicalize_log.json',
            'check/check_report.json', 'verify/verification_findings.json',
            'coverage/coverage_findings.json', 'gates/gates_report.json',
            'enrich/deterministic_enrichment.json', 'export/deal_events.csv']:
    p = base / sub
    assert p.exists(), f'Missing: {p}'
# Gates has no blockers
with open(base / 'gates/gates_report.json') as f:
    gr = json.load(f)
    blockers = [f for f in gr.get('findings', []) if f.get('severity') == 'blocker']
    assert len(blockers) == 0, f'Gates has {len(blockers)} blocker(s)'
# Export CSV is non-empty
csv_path = base / 'export/deal_events.csv'
lines = csv_path.read_text().strip().split('\n')
assert len(lines) >= 2, f'deal_events.csv has only {len(lines)} lines'
print(f'Zep pipeline PASSED: {len(lines)-1} event rows exported')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/skill/zep/extract/spans.json exists (canonical form)
    - data/skill/zep/canonicalize/canonicalize_log.json exists
    - data/skill/zep/check/check_report.json exists with no blockers
    - data/skill/zep/verify/verification_findings.json exists
    - data/skill/zep/coverage/coverage_findings.json exists
    - data/skill/zep/gates/gates_report.json exists with zero blocker findings
    - data/skill/zep/enrich/deterministic_enrichment.json exists
    - data/skill/zep/export/deal_events.csv exists with >= 2 lines (header + data)
    - data/pipeline.duckdb has been updated with Zep data
  </acceptance_criteria>
  <done>Zep passes full deterministic pipeline from canonicalize through db-export with no blocker findings</done>
</task>

</tasks>

<verification>
1. EXTRACT-04: No `bidder_new_mountain_capital` in any 2014-cycle grouped proposal/drop event actor_ids
2. RERUN-01 (partial): All deterministic pipeline stages complete without errors for Zep
3. All artifact files exist at expected paths under data/skill/zep/
4. deal_events.csv is generated from DuckDB (not hand-crafted)
</verification>

<success_criteria>
- Zep events_raw.json does not include NMC in 2014-cycle grouped events (EXTRACT-04)
- All 8 deterministic stages (canonicalize, check, verify, coverage, gates, enrich-core, db-load, db-export) pass without errors
- deal_events.csv contains event rows with corrected actor attributions
</success_criteria>

<output>
After completion, create `.planning/phases/09-deal-specific-fixes-revalidation/09-01-SUMMARY.md`
</output>

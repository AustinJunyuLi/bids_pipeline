---
phase: 13-validation-stack
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - skill_pipeline/models.py
  - skill_pipeline/coverage_v2.py
  - tests/test_skill_coverage_v2.py
autonomous: true
requirements: [VALID-02]

must_haves:
  truths:
    - "`coverage_v2` emits structured `CoverageCheckRecord` rows for covered and uncovered source cues"
    - "v2 coverage records carry observation-aware provenance instead of free-text notes"
  artifacts:
    - path: "skill_pipeline/coverage_v2.py"
      provides: "v2 structured coverage runtime"
    - path: "tests/test_skill_coverage_v2.py"
      provides: "source-cue to observation-coverage regression tests"
  key_links:
    - from: "skill_pipeline/coverage_v2.py"
      to: "tests/test_skill_coverage_v2.py"
      via: "shared synthetic source + canonical observation fixtures"
      pattern: "coverage statuses are exercised for observed, ambiguous, and not_found cases"
---

<objective>
Make v2 coverage a first-class structured output rather than a sparse text
audit.

Purpose: The observation graph should be measurable against filing-derived
source cues before derivation, with machine-readable status and provenance on
every record.

Output: `coverage_v2.py` plus dedicated coverage tests.
</objective>

<context>
@.planning/phases/13-validation-stack/13-CONTEXT.md
@.planning/phases/13-validation-stack/13-RESEARCH.md
@skill_pipeline/coverage.py
@skill_pipeline/models.py
@skill_pipeline/models_v2.py
@skill_pipeline/extract_artifacts_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2 coverage generation and summary writing</name>
  <files>
    skill_pipeline/models.py,
    skill_pipeline/coverage_v2.py
  </files>
  <action>
Add `coverage_v2.py` that:
- reuses source cue detection from `coverage.py`
- matches cues against canonical v2 observations
- emits structured `CoverageCheckRecord` rows with v2 observation/span
  provenance
- writes `coverage_findings.json` and `coverage_summary.json`
  </action>
  <acceptance_criteria>
    - observed cues remain visible in the structured output
    - uncovered high-confidence critical cues fail the summary
    - ambiguous matches are surfaced deterministically
  </acceptance_criteria>
  <done>v2 coverage becomes a deterministic report surface, not prose</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage regression tests</name>
  <files>
    tests/test_skill_coverage_v2.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_coverage_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - tests cover observed coverage
    - tests cover not_found coverage for critical cues
    - tests cover ambiguous coverage with multiple candidate observations
  </acceptance_criteria>
  <done>Phase 13 coverage behavior is pinned by synthetic source fixtures</done>
</task>

</tasks>

---
phase: 13-validation-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/models.py
  - skill_pipeline/models_v2.py
  - skill_pipeline/check_v2.py
  - tests/test_skill_check_v2.py
autonomous: true
requirements: [VALID-01]

must_haves:
  truths:
    - "`check_v2` rejects canonical observation graphs with missing evidence, unresolved refs, or bidderless proposals"
    - "v2 structural findings are typed with observation-aware identifiers"
  artifacts:
    - path: "skill_pipeline/check_v2.py"
      provides: "v2 structural validation runtime"
    - path: "skill_pipeline/models_v2.py"
      provides: "v2 check report typing"
    - path: "tests/test_skill_check_v2.py"
      provides: "synthetic failing-input coverage for structural validation"
  key_links:
    - from: "skill_pipeline/check_v2.py"
      to: "tests/test_skill_check_v2.py"
      via: "canonical-v2 fixture mutations"
      pattern: "each required blocker condition is pinned by a direct failing fixture"
---

<objective>
Land the structural v2 validation surface first.

Purpose: Derivation must never consume a broken observation graph, so the first
v2 gate must catch missing evidence and broken references with typed JSON
output.

Output: `check_v2.py`, v2 report typing, and focused structural tests.
</objective>

<context>
@.planning/phases/13-validation-stack/13-CONTEXT.md
@.planning/phases/13-validation-stack/13-RESEARCH.md
@skill_pipeline/check.py
@skill_pipeline/models.py
@skill_pipeline/models_v2.py
@skill_pipeline/extract_artifacts_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add observation-aware structural report models and runtime</name>
  <files>
    skill_pipeline/models.py,
    skill_pipeline/models_v2.py,
    skill_pipeline/check_v2.py
  </files>
  <action>
Add a v2 structural check runtime that loads canonical observation artifacts and
emits blocker/warning findings for:
- observations missing `evidence_span_ids`
- unresolved party/cohort references
- unresolved observation references
- proposals without bidder subjects
- agreements whose supersession links do not point to agreements
  </action>
  <acceptance_criteria>
    - canonical v2 input is required
    - report JSON uses observation-aware identifier fields
    - pass/fail summary matches blocker presence
  </acceptance_criteria>
  <done>`check_v2` can fail a broken observation graph before derivation</done>
</task>

<task type="auto">
  <name>Task 2: Add structural regression tests</name>
  <files>
    tests/test_skill_check_v2.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_check_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - tests cover missing observation evidence spans
    - tests cover unresolved refs
    - tests cover bidderless proposals and bad supersession typing
  </acceptance_criteria>
  <done>Phase 13 structural validation is pinned by synthetic canonical fixtures</done>
</task>

</tasks>

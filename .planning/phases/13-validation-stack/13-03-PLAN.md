---
phase: 13-validation-stack
plan: 03
type: execute
wave: 1
depends_on: [13-01, 13-02]
files_modified:
  - skill_pipeline/models_v2.py
  - skill_pipeline/gates_v2.py
  - skill_pipeline/cli.py
  - tests/test_skill_gates_v2.py
  - tests/test_skill_pipeline.py
  - CLAUDE.md
autonomous: true
requirements: [VALID-03]

must_haves:
  truths:
    - "`gates_v2` enforces graph-level invariants that structural checks cannot see"
    - "the CLI exposes additive `check-v2`, `coverage-v2`, and `gates-v2` commands"
    - "repo memory distinguishes reserved v2 paths from live v2 validation writers"
  artifacts:
    - path: "skill_pipeline/gates_v2.py"
      provides: "v2 semantic gate runtime"
    - path: "skill_pipeline/cli.py"
      provides: "CLI registration for v2 validation commands"
    - path: "tests/test_skill_gates_v2.py"
      provides: "graph-level gate regression tests"
    - path: "tests/test_skill_pipeline.py"
      provides: "CLI parser coverage for v2 validation commands"
    - path: "CLAUDE.md"
      provides: "updated repo memory for live Phase 13 v2 validation surfaces"
  key_links:
    - from: "skill_pipeline/gates_v2.py"
      to: "tests/test_skill_gates_v2.py"
      via: "synthetic graph-cycle, deadline, and cohort fixtures"
      pattern: "each required graph invariant is enforced by a direct blocker test"
---

<objective>
Finish the v2 validation stack with semantic gates and CLI exposure.

Purpose: Phase 13 is complete only when all three v2 validation layers are live
runtime commands that write JSON artifacts and can block downstream derivation.

Output: `gates_v2.py`, CLI wiring, parser tests, and repo-memory updates.
</objective>

<context>
@.planning/phases/13-validation-stack/13-CONTEXT.md
@.planning/phases/13-validation-stack/13-RESEARCH.md
@skill_pipeline/gates.py
@skill_pipeline/cli.py
@skill_pipeline/models_v2.py
@skill_pipeline/extract_artifacts_v2.py
@tests/test_skill_pipeline.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2 graph gates and CLI wiring</name>
  <files>
    skill_pipeline/models_v2.py,
    skill_pipeline/gates_v2.py,
    skill_pipeline/cli.py
  </files>
  <action>
Add `gates_v2.py` with blockers for:
- revision/supersession cycles
- cohort child counts greater than parent counts
- solicitation due dates before solicitation dates

Register additive `check-v2`, `coverage-v2`, and `gates-v2` CLI commands.
  </action>
  <acceptance_criteria>
    - gate report summary fails on blocker findings
    - CLI dispatch reaches all three v2 validation runtimes
    - v1 CLI behavior remains unchanged
  </acceptance_criteria>
  <done>all Phase 13 validation commands are live and callable</done>
</task>

<task type="auto">
  <name>Task 2: Add graph-gate tests and update memory</name>
  <files>
    tests/test_skill_gates_v2.py,
    tests/test_skill_pipeline.py,
    CLAUDE.md
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_gates_v2.py tests/test_skill_pipeline.py -k 'check_v2 or coverage_v2 or gates_v2 or check-v2 or coverage-v2 or gates-v2'</automated>
  </verify>
  <acceptance_criteria>
    - tests cover cycles, deadline ordering, and cohort-parent count blockers
    - parser tests cover all v2 validation subcommands
    - CLAUDE.md marks the Phase 13 validation writers as live runtime outputs
  </acceptance_criteria>
  <done>Phase 13 semantic gates are tested and documented</done>
</task>

</tasks>

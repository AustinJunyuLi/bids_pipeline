---
phase: 24-v1-retirement-git-history-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - CLAUDE.md
  - docs/design.md
  - docs/HOME_COMPUTER_SETUP.md
  - skill_pipeline/cli.py
  - skill_pipeline/compose_prompts.py
  - skill_pipeline/check_v2.py
  - skill_pipeline/coverage_v2.py
  - skill_pipeline/db_export_v2.py
  - skill_pipeline/deal_agent.py
  - skill_pipeline/extract_artifacts_v2.py
  - skill_pipeline/models_v2.py
  - skill_pipeline/normalize/dates.py
  - skill_pipeline/paths.py
  - skill_pipeline/provenance.py
  - skill_pipeline/seeds.py
  - tests/test_skill_mirror_sync.py
  - tests/test_runtime_contract_docs.py
  - tests/test_benchmark_separation_policy.py
  - tests/test_skill_pipeline.py
  - tests/test_skill_db_export_v2.py
autonomous: true
requirements: [RETIRE-01, RETIRE-02, RETIRE-03, RETIRE-04]

must_haves:
  truths:
    - "the working tree exposes only the live v2 runtime and skill surface"
    - "live v2 code no longer imports legacy v1 runtime modules"
    - "GitHub-visible history includes an explicit recovery tag for the last pre-retirement tree"
    - "tests and docs are rewritten for a v2-only working tree"
  artifacts:
    - path: "skill_pipeline/cli.py"
      provides: "v2-only public CLI surface"
    - path: "skill_pipeline/compose_prompts.py"
      provides: "v2-only prompt composition contract"
    - path: "skill_pipeline/paths.py"
      provides: "live path contract without legacy archive/output paths"
    - path: "CLAUDE.md"
      provides: "authoritative v2-only repo contract plus recovery guidance"
    - path: "tests/test_skill_mirror_sync.py"
      provides: "skill mirror expectations without legacy skill presence"
    - path: "tests/test_runtime_contract_docs.py"
      provides: "doc contract coverage for v1 retirement"
    - path: "tests/test_benchmark_separation_policy.py"
      provides: "benchmark-boundary coverage for the v2-only doc set"
  key_links:
    - from: "skill_pipeline/models_v2.py"
      to: "skill_pipeline/check_v2.py"
      via: "shared summaries and common types come from neutral modules"
      pattern: "v2 runtime no longer depends on `skill_pipeline.models`"
    - from: "skill_pipeline/db_export_v2.py"
      to: "tests/test_skill_db_export_v2.py"
      via: "export ordering comes from a neutral constant module"
      pattern: "legacy db-export import path disappears without changing live v2 output order"
    - from: "CLAUDE.md"
      to: "tests/test_runtime_contract_docs.py"
      via: "repo docs explain Git-history recovery instead of working-tree legacy preservation"
      pattern: "the authoritative contract stays accurate after v1 removal"
---

<objective>
Retire the legacy v1 working-tree surface while preserving recoverability.

Purpose: the repo has already cut over to v2, but legacy commands, skills,
tests, data archives, and some live imports still keep v1 physically present.
This plan removes that surface, extracts any shared live dependencies into
neutral modules, and records a concrete recovery path through Git history.

Output: a v2-only working tree, an explicit recovery tag, and updated tests/docs.
</objective>

<context>
@.planning/phases/24-v1-retirement-git-history-preservation/24-CONTEXT.md
@CLAUDE.md
@skill_pipeline/cli.py
@skill_pipeline/models.py
@skill_pipeline/paths.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple live v2 code from legacy runtime modules</name>
  <files>
    skill_pipeline/cli.py,
    skill_pipeline/compose_prompts.py,
    skill_pipeline/check_v2.py,
    skill_pipeline/coverage_v2.py,
    skill_pipeline/db_export_v2.py,
    skill_pipeline/deal_agent.py,
    skill_pipeline/extract_artifacts_v2.py,
    skill_pipeline/models_v2.py,
    skill_pipeline/normalize/dates.py,
    skill_pipeline/paths.py,
    skill_pipeline/provenance.py,
    skill_pipeline/seeds.py
  </files>
  <action>
Extract the shared live-only schemas, path models, and export-order constants out
of legacy-labeled modules. Then repoint the active v2 runtime so it no longer
imports `models.py`, `coverage.py`, `db_export.py`, or other removed legacy
runtime files.
  </action>
  <acceptance_criteria>
    - live v2 modules import only neutral shared modules plus v2-specific modules
    - the public CLI exposes only live v2 commands
    - compose-prompts becomes a v2-only entry point
  </acceptance_criteria>
  <done>the live runtime no longer depends on the legacy runtime tree</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy working-tree surfaces and document recovery</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/STATE.md,
    CLAUDE.md,
    docs/design.md,
    docs/HOME_COMPUTER_SETUP.md,
    tests/test_skill_mirror_sync.py,
    tests/test_runtime_contract_docs.py,
    tests/test_benchmark_separation_policy.py,
    tests/test_skill_pipeline.py,
    tests/test_skill_db_export_v2.py
  </files>
  <verify>
    <automated>python scripts/sync_skill_mirrors.py --check</automated>
    <automated>pytest -q tests/test_skill_mirror_sync.py tests/test_runtime_contract_docs.py tests/test_benchmark_separation_policy.py tests/test_skill_pipeline.py tests/test_skill_db_export_v2.py</automated>
  </verify>
  <action>
Delete the legacy skill directories, mirrored copies, v1-only tests, scripts,
prompt assets, and archived `data/legacy/v1/` artifacts from the working tree.
Before finalizing that removal, create and push an explicit Git tag pointing at
commit `82a4966` and update repo docs plus phase/state artifacts to tell users
how to recover the retired v1 tree from GitHub history.
  </action>
  <acceptance_criteria>
    - only live v2 skills remain under `.claude/skills/` and mirrors
    - docs stop describing a working-tree legacy split and instead document Git-history recovery
    - focused tests pass against the v2-only working tree
  </acceptance_criteria>
  <done>v1 is absent from the working tree but explicitly recoverable from Git history</done>
</task>

</tasks>

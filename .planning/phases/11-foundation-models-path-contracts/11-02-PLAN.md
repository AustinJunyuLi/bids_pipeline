---
phase: 11-foundation-models-path-contracts
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - skill_pipeline/models.py
  - skill_pipeline/paths.py
  - tests/test_skill_pipeline.py
  - CLAUDE.md
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "SkillPathSet exposes additive v2 path fields while every existing v1 path value remains unchanged"
    - "build_skill_paths() and ensure_output_directories() materialize the new v2 directories"
    - "Repository memory documents the new additive v2 path surface"
  artifacts:
    - path: "skill_pipeline/models.py"
      provides: "Updated SkillPathSet with additive v2 path fields"
    - path: "skill_pipeline/paths.py"
      provides: "Path builder and directory creation for v2 paths"
    - path: "tests/test_skill_pipeline.py"
      provides: "Regression tests for additive v2 path exposure"
    - path: "CLAUDE.md"
      provides: "Updated repo memory for the new additive v2 path contract"
  key_links:
    - from: "skill_pipeline/models.py"
      to: "skill_pipeline/paths.py"
      via: "SkillPathSet additions consumed by build_skill_paths()"
      pattern: "schema and path builder evolve together"
---

<objective>
Add the v2 path contract additively and document it in the repo authority file.

Purpose: Later phases need stable v2 artifact locations, but Phase 11 must not
disturb any existing v1 paths.

Output: additive v2 path fields, path tests, and updated CLAUDE.md guidance.
</objective>

<context>
@.planning/phases/11-foundation-models-path-contracts/11-CONTEXT.md
@.planning/phases/11-foundation-models-path-contracts/11-RESEARCH.md
@skill_pipeline/models.py
@skill_pipeline/paths.py
@tests/test_skill_pipeline.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive v2 path fields to SkillPathSet and build_skill_paths()</name>
  <files>
    skill_pipeline/models.py,
    skill_pipeline/paths.py
  </files>
  <action>
Add the minimum additive v2 path contract needed by later phases, including the
required `extract_v2/` and `export_v2/` directories plus key file paths inside
those directories if helpful.

Preserve every existing v1 field and value.
  </action>
  <acceptance_criteria>
    - existing v1 path fields remain unchanged
    - new v2 directory fields exist on `SkillPathSet`
    - `ensure_output_directories()` creates the new directories
  </acceptance_criteria>
  <done>SkillPathSet exposes additive v2 path locations without disturbing v1</done>
</task>

<task type="auto">
  <name>Task 2: Add additive path tests and update CLAUDE.md</name>
  <files>
    tests/test_skill_pipeline.py,
    CLAUDE.md
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_pipeline.py -k 'path or prompt_paths or v2'</automated>
  </verify>
  <acceptance_criteria>
    - tests assert the new v2 paths
    - tests assert legacy prompt/v1 paths are unchanged
    - CLAUDE.md documents the additive v2 path surface and keeps v1 contract notes intact
  </acceptance_criteria>
  <done>Phase 11 path contract is tested and documented</done>
</task>

</tasks>

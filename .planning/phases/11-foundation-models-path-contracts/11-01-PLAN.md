---
phase: 11-foundation-models-path-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill_pipeline/models_v2.py
  - tests/test_skill_observation_models.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06]

must_haves:
  truths:
    - "A new additive `skill_pipeline.models_v2` module defines the full Phase 11 v2 schema surface"
    - "All six observation subtypes deserialize through a discriminated union on `obs_type`"
    - "Derived record schemas require `DerivationBasis` provenance"
  artifacts:
    - path: "skill_pipeline/models_v2.py"
      provides: "Importable v2 schema module"
    - path: "tests/test_skill_observation_models.py"
      provides: "Model validation and round-trip tests for the v2 schema surface"
  key_links:
    - from: "skill_pipeline/models_v2.py"
      to: "tests/test_skill_observation_models.py"
      via: "Pydantic validation and serialization tests"
      pattern: "Schema definitions are proven by direct model construction"
---

<objective>
Create the additive v2 schema module and its direct validation tests.

Purpose: Every later observation-graph phase depends on these types existing
first, but no runtime behavior should change yet.

Output: `skill_pipeline/models_v2.py` plus focused tests proving the schema
surface works as designed.
</objective>

<context>
@.planning/phases/11-foundation-models-path-contracts/11-CONTEXT.md
@.planning/phases/11-foundation-models-path-contracts/11-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@skill_pipeline/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the additive v2 schema module</name>
  <files>
    skill_pipeline/models_v2.py
  </files>
  <action>
Create `skill_pipeline/models_v2.py` with:
- `PartyRecord`
- `CohortRecord`
- six observation subtypes plus `Observation` discriminated union
- `DerivationBasis`
- `ProcessPhaseRecord`, `LifecycleTransitionRecord`, `CashRegimeRecord`,
  `JudgmentRecord`, and `AnalystRowRecord`
- v2 artifact wrapper models

Reuse shared primitives where appropriate; do not mutate the v1 schemas.
  </action>
  <acceptance_criteria>
    - `import skill_pipeline.models_v2` succeeds
    - the module contains all required Phase 11 schema names
  </acceptance_criteria>
  <done>models_v2.py defines the additive v2 schema surface</done>
</task>

<task type="auto">
  <name>Task 2: Add focused v2 model validation tests</name>
  <files>
    tests/test_skill_observation_models.py
  </files>
  <verify>
    <automated>pytest -q tests/test_skill_observation_models.py</automated>
  </verify>
  <acceptance_criteria>
    - tests cover PartyRecord, CohortRecord, the observation union, cross-observation reference fields, and derived-record provenance
    - round-trip serialization preserves key fields
  </acceptance_criteria>
  <done>Phase 11 schema tests prove the additive v2 model surface</done>
</task>

</tasks>

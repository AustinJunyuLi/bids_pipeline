---
phase: 06-chunked-extraction-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/enrich-deal/SKILL.md
autonomous: true
requirements:
  - WFLO-03

must_haves:
  truths:
    - "enrich-deal SKILL.md describes event-targeted re-reads with scoped context per subtask"
    - "Dropout classification uses one call per drop event with evidence blocks + round context (~2-3K tokens)"
    - "Initiation judgment uses one call reading first 10-15 blocks only"
    - "Deterministic enrichment tasks (rounds, cycles, formal_boundary, bid_classifications) remain in enrich-core and are NOT duplicated in enrich-deal"
  artifacts:
    - path: ".claude/skills/enrich-deal/SKILL.md"
      provides: "Event-targeted enrichment skill contract"
      contains: "event-targeted re-read"
  key_links:
    - from: ".claude/skills/enrich-deal/SKILL.md"
      to: "data/skill/<slug>/enrich/enrichment.json"
      via: "Writes section output contract"
      pattern: "enrichment\\.json"
---

<objective>
Refactor enrich-deal SKILL.md to use event-targeted re-reads instead of full-context LLM calls.

Purpose: The current enrich-deal loads the full chronology context for every enrichment subtask. With event-targeted re-reads, each subtask receives only the blocks around the events it needs to analyze (~2-3K tokens per call instead of the full chronology). This is cheaper, faster, and produces better focused judgments.

Output: Updated `.claude/skills/enrich-deal/SKILL.md` with event-targeted re-read instructions for each interpretive subtask.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chunked-extraction-architecture/06-CONTEXT.md
@.claude/skills/enrich-deal/SKILL.md
@skill_pipeline/enrich_core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor enrich-deal SKILL.md to event-targeted re-reads</name>
  <files>.claude/skills/enrich-deal/SKILL.md</files>
  <read_first>
    - .claude/skills/enrich-deal/SKILL.md (current enrichment contract with all 8 tasks)
    - skill_pipeline/enrich_core.py (deterministic enrichment -- rounds, bid_classifications, cycles, formal_boundary -- to confirm what stays deterministic)
    - .planning/phases/06-chunked-extraction-architecture/06-CONTEXT.md (enrichment refactor decisions: event-targeted re-reads, scoped context per subtask)
  </read_first>
  <action>
Refactor `.claude/skills/enrich-deal/SKILL.md` to add event-targeted re-read instructions. The overall structure of 8 tasks stays the same. The execution order stays the same (3 -> 4 -> 5 -> 1 -> 2 -> 6 -> 7 -> 8).

**Sections to preserve unchanged:**
- Frontmatter (name, description)
- Design Principles
- Purpose
- When To Use (including "Deterministic core" note)
- Benchmark Boundary
- Reads table
- Writes
- JSON Format (entire example and field reference)
- Gate

**Changes to the Enrichment Tasks section:**

Add a new subsection before Task 1 titled "### Context Scoping Protocol":

```
### Context Scoping Protocol

Each enrichment subtask below uses event-targeted re-reads instead of loading the
full chronology. For each subtask:

1. Identify the events or actors the subtask needs to analyze.
2. For each event, find its evidence_refs -> extract block_ids.
3. Load only those blocks from chronology_blocks.jsonl, plus 1-2 surrounding
   blocks for narrative context.
4. Pass only this scoped block window (~2-3K tokens) to the LLM call.

This keeps each subtask focused and prevents context dilution from unrelated
narrative.
```

**Modify Task 1 (Dropout Classification):**

Add after the existing intro paragraph and before the classification table:

```
**Scoped context:** For each drop event, load:
- The blocks referenced in the drop event's evidence_refs
- 1-2 blocks before and after for narrative flow
- The blocks from the most recent round announcement event (for round context)
- Prior proposal events for this actor (for DropBelowInf/DropAtInf comparison)

One LLM call per drop event. Each call receives ~2-3K tokens of scoped context.
```

**Modify Task 6 (Initiation Judgment):**

Add after "Determine who started the sale process":

```
**Scoped context:** Load the first 10-15 blocks of chronology_blocks.jsonl only.
The initiation judgment depends on the earliest events in the timeline; later
narrative is not needed.

One LLM call for initiation judgment.
```

**Modify Task 7 (Advisory Attribution Verification):**

Add after "For each actor with `role: "advisor"`":

```
**Scoped context:** For each advisor, load:
- The blocks referenced in the advisor's evidence_refs
- 1-2 surrounding blocks for the passage establishing the advisory relationship

One LLM call per advisor actor.
```

**Modify Task 8 (Count Reconciliation):**

Add after "This output is diagnostic":

```
**Scoped context:** Load:
- Blocks referenced in each count_assertion's evidence_refs
- The full actor roster (structured JSON, not filing text)

One LLM call for all count reconciliation together.
```

**Tasks 2, 3, 4, 5 (Bid Classification, Round Structure, Cycle Segmentation, Formal Boundary):**

These tasks remain in `enrich-core` (deterministic). Add a note to each:

```
**Note:** This task is computed deterministically by `skill-pipeline enrich-core`.
The skill reads the deterministic output. No LLM call needed.
```

Keep the existing task descriptions intact -- they document the logic that enrich-core implements. Just add the note.
  </action>
  <verify>
    <automated>grep -c "Context Scoping Protocol" .claude/skills/enrich-deal/SKILL.md && grep -c "event-targeted re-read" .claude/skills/enrich-deal/SKILL.md && grep -c "Scoped context" .claude/skills/enrich-deal/SKILL.md && grep -c "computed deterministically" .claude/skills/enrich-deal/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/enrich-deal/SKILL.md contains "### Context Scoping Protocol"
    - .claude/skills/enrich-deal/SKILL.md contains "event-targeted re-read" (at least once)
    - .claude/skills/enrich-deal/SKILL.md Task 1 contains "Scoped context:" with "One LLM call per drop event"
    - .claude/skills/enrich-deal/SKILL.md Task 6 contains "Scoped context:" with "first 10-15 blocks"
    - .claude/skills/enrich-deal/SKILL.md Task 7 contains "Scoped context:" with "One LLM call per advisor"
    - .claude/skills/enrich-deal/SKILL.md Task 8 contains "Scoped context:" with "count_assertion"
    - .claude/skills/enrich-deal/SKILL.md Tasks 2, 3, 4, 5 contain "computed deterministically by `skill-pipeline enrich-core`"
    - .claude/skills/enrich-deal/SKILL.md still contains "Execution order: 3 -> 4 -> 5 -> 1 -> 2 -> 6 -> 7 -> 8"
    - .claude/skills/enrich-deal/SKILL.md still contains all 8 task headings (Task 1 through Task 8)
    - .claude/skills/enrich-deal/SKILL.md still contains "## Gate" section
    - .claude/skills/enrich-deal/SKILL.md still contains JSON Format example
  </acceptance_criteria>
  <done>enrich-deal SKILL.md describes event-targeted re-reads for each interpretive subtask. Deterministic tasks are marked as enrich-core computed. Execution order and output contract unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Sync skill mirrors after enrich-deal SKILL.md update</name>
  <files>.codex/skills/enrich-deal/SKILL.md, .cursor/skills/enrich-deal/SKILL.md</files>
  <read_first>
    - scripts/sync_skill_mirrors.py (sync script to run)
    - .claude/skills/enrich-deal/SKILL.md (canonical source just updated)
  </read_first>
  <action>
Run `python scripts/sync_skill_mirrors.py` to propagate the updated enrich-deal SKILL.md to `.codex/skills/` and `.cursor/skills/` mirrors.

Then verify with `python scripts/sync_skill_mirrors.py --check` that all mirrors are in sync.
  </action>
  <verify>
    <automated>python scripts/sync_skill_mirrors.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/sync_skill_mirrors.py --check` exits 0 with no drift detected
    - .codex/skills/enrich-deal/SKILL.md contains "Context Scoping Protocol"
    - .cursor/skills/enrich-deal/SKILL.md contains "Context Scoping Protocol"
  </acceptance_criteria>
  <done>All skill mirrors are in sync with the updated canonical enrich-deal SKILL.md.</done>
</task>

</tasks>

<verification>
- `grep -c "Context Scoping Protocol" .claude/skills/enrich-deal/SKILL.md` returns 1
- `grep -c "event-targeted re-read" .claude/skills/enrich-deal/SKILL.md` returns >= 1
- `python scripts/sync_skill_mirrors.py --check` exits 0
- `pytest -q tests/test_skill_mirror_sync.py` passes
</verification>

<success_criteria>
enrich-deal SKILL.md describes event-targeted re-reads for dropout classification, initiation judgment, advisory verification, and count reconciliation. Deterministic tasks (rounds, bid_classifications, cycles, formal_boundary) are marked as enrich-core computed. Skill mirrors are in sync. No output contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chunked-extraction-architecture/06-03-SUMMARY.md`
</output>

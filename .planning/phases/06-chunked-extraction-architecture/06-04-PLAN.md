---
phase: 06-chunked-extraction-architecture
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified: []
autonomous: false
requirements:
  - WFLO-03
  - QUAL-01

must_haves:
  truths:
    - "stec extracts through chunked path producing actors_raw.json + events_raw.json in the same schema"
    - "petsmart-inc extracts through chunked path and captures silent NDA signers via count_assertions"
    - "canonicalize, check, verify, coverage all pass on both deals after chunked extraction"
    - "All existing pytest tests pass after the full phase changes"
  artifacts: []
  key_links:
    - from: ".claude/skills/extract-deal/SKILL.md"
      to: "data/skill/stec/extract/actors_raw.json"
      via: "Chunked extraction produces artifacts"
      pattern: "actors_raw\\.json"
    - from: "skill_pipeline/canonicalize.py"
      to: "data/skill/stec/extract/actors_raw.json"
      via: "canonicalize with _dedup_actors processes chunked output"
      pattern: "actors_raw_path"
---

<objective>
End-to-end validation of chunked extraction on stec (largest deal, 234 blocks, ~22K tokens) and petsmart-inc (silent NDA signer test case, 35 blocks, 15 NDA / 9 unnamed parties).

Purpose: Validate that the chunked extraction path, deterministic safety nets, and existing downstream stages work together on real deals. stec is the stress test (most chunks). petsmart is the quality test (unnamed party recovery depends on count_assertion capture).

Output: Validated extraction artifacts for both deals. Human verification of extraction quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chunked-extraction-architecture/06-CONTEXT.md
@.planning/phases/06-chunked-extraction-architecture/06-01-SUMMARY.md
@.planning/phases/06-chunked-extraction-architecture/06-02-SUMMARY.md
@.planning/phases/06-chunked-extraction-architecture/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite to confirm all phase changes are clean</name>
  <files></files>
  <read_first>
    - .planning/phases/06-chunked-extraction-architecture/06-02-SUMMARY.md (confirms which Python files were changed)
  </read_first>
  <action>
Run the full pytest suite to confirm that all changes from Plans 01-03 are clean:

```bash
pytest -q
```

This validates:
- All existing canonicalize tests pass (including with the new _dedup_actors call in run_canonicalize)
- All existing check tests pass (including with the new _check_actor_audit call in run_check)
- Skill mirror sync tests pass (mirrors updated in Plans 01 and 03)
- Pipeline integration tests pass
- Benchmark separation policy tests pass

If any test fails, diagnose and fix before proceeding.
  </action>
  <verify>
    <automated>cd /home/austinli/Projects/bids_data && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` exits 0
    - No test failures or errors in output
  </acceptance_criteria>
  <done>Full test suite passes with all phase changes applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of chunked extraction on stec and petsmart</name>
  <files></files>
  <action>
Present the verification checklist to the user. The user will run the extraction and deterministic pipeline on stec and petsmart-inc, then verify quality.

What was built in Plans 01-03:
- extract-deal SKILL.md rewritten for all-chunked sequential extraction with consolidation
- canonicalize.py gained _dedup_actors (merges actors by canonical_name/role/bidder_kind)
- check.py gained _check_actor_audit (flags duplicate names, bidders without NDAs, count gaps, advisors without advised_actor_id)
- enrich-deal SKILL.md refactored for event-targeted re-reads
- All skill mirrors synced
- Full test suite passes

Steps for verification:

1. Run `/extract-deal stec` using the updated chunked extraction SKILL.md
   - Confirm it produces 6-7 chunks (stec has 234 blocks at ~22K tokens)
   - Confirm `data/skill/stec/extract/actors_raw.json` and `events_raw.json` exist
   - Confirm no single-shot fallback was used

2. Run the deterministic pipeline on stec:
   ```
   skill-pipeline canonicalize --deal stec
   skill-pipeline check --deal stec
   skill-pipeline verify --deal stec
   skill-pipeline coverage --deal stec
   ```
   - All four stages should complete without blocker-level failures
   - Check report should show actor audit findings (warnings expected, not blockers)

3. Run `/extract-deal petsmart-inc` using the updated chunked extraction SKILL.md
   - Confirm it produces 2-3 chunks (petsmart has 35 blocks)
   - Confirm count_assertions capture NDA signer counts (target: "15 parties signed confidentiality agreements" or similar)

4. Run the deterministic pipeline on petsmart:
   ```
   skill-pipeline canonicalize --deal petsmart-inc
   skill-pipeline check --deal petsmart-inc
   ```
   - Canonicalize should trigger unnamed-party recovery using the captured count_assertions
   - Check actor audit should report count_assertion findings if gap exists

5. Verify output contract: open `data/skill/stec/extract/actors_raw.json` and confirm it matches the existing schema (actors array, count_assertions array, unresolved_mentions array).
  </action>
  <verify>
    <automated>test -f data/skill/stec/extract/actors_raw.json && test -f data/skill/stec/extract/events_raw.json && test -f data/skill/petsmart-inc/extract/actors_raw.json && test -f data/skill/petsmart-inc/extract/events_raw.json && echo "artifacts exist"</automated>
  </verify>
  <acceptance_criteria>
    - data/skill/stec/extract/actors_raw.json exists and contains "actors" array
    - data/skill/stec/extract/events_raw.json exists and contains "events" array
    - data/skill/petsmart-inc/extract/actors_raw.json exists and contains "actors" array
    - data/skill/petsmart-inc/extract/events_raw.json exists and contains "events" array
    - Human approves extraction quality for both deals
  </acceptance_criteria>
  <done>Both stec and petsmart-inc extracted through chunked path. Deterministic stages processed output. Human approved quality.</done>
</task>

</tasks>

<verification>
- `pytest -q` passes (full suite)
- stec extracts through chunked path (6-7 chunks)
- petsmart-inc extracts through chunked path (2-3 chunks)
- canonicalize + check + verify + coverage pass on stec
- canonicalize + check pass on petsmart-inc with unnamed-party recovery triggered
- Human approves extraction quality on both deals
</verification>

<success_criteria>
Both stec and petsmart-inc extract through the chunked path. Deterministic stages process the output without blocker failures. Count assertions are captured for petsmart. Test suite passes. Human approves quality.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chunked-extraction-architecture/06-04-SUMMARY.md`
</output>

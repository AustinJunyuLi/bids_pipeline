# Machine-local tooling/editor/agent settings only.
# Do not treat this file as the Python runtime contract for the repo.
#
# Put the SEC runtime identity in your shell profile instead:
#   PowerShell: $env:PIPELINE_SEC_IDENTITY = "Your Name your.email@example.com"
#   bash/zsh:   export PIPELINE_SEC_IDENTITY="Your Name your.email@example.com"

NEWAPI_API_KEY=
NEWAPI_BASE_URL=
NEWAPI_MODEL=
NEWAPI_REASONING_EFFORT=
NEWAPI_SERVICE_TIER=
